# Apply Progress — LLM Provider Admin (DB-backed, admin-switchable provider credentials)

## Workload / PR Boundary

- Completed slices: **PR A — Migration infra** (Work Units 1–2) and **PR B — Crypto + admin backend** (Work Units 3–4). This is now 2 of the 4-PR chain (`tasks.md`'s Suggested PR Chain: PR A migration infra → PR B crypto + admin backend → PR C pipeline wiring + worker → PR D Angular admin UI + provenance + manual verification).
- Delivery strategy: `auto-chain`, chain strategy `stacked-to-main`, pre-approved by `tasks.md`'s own Review Workload Forecast (`400-line budget risk: High`, `Decision needed before apply: No`) — same forecast pattern as `mvp-vertical-slice`.
- PR A estimated authored change: **351 lines** (`git diff --stat` on tracked files: `apps/api/src/db/migrate.mjs` +34/-12, `apps/api/tests/migrate-runner.test.mjs` +181/-2 = 217 changed; new untracked files `apps/api/src/db/migrations/0002_llm_provider_config.sql` 18 lines + `apps/api/tests/llm-provider-config-migration.test.mjs` 116 lines = 134). Well inside the 400-line budget — matches the precedent set by `mvp-vertical-slice`'s own PR A (~239 lines).
- PR A scope guard: touched only `apps/api/src/db/migrate.mjs`, `apps/api/tests/migrate-runner.test.mjs`, and two new files (`apps/api/src/db/migrations/0002_llm_provider_config.sql`, `apps/api/tests/llm-provider-config-migration.test.mjs`). `infra/docker-compose.yml` was temporarily edited for live verification (port remap) and reverted before finishing — `git diff infra/docker-compose.yml` confirms zero net change. No encryption, admin endpoints, worker changes, or Angular UI were touched — those are Work Units 3+ (PR B/C/D), explicitly out of scope for this pass.
- PR B estimated authored change: **~1,152 lines** (`app.module.ts` +5/-2 = 7 changed; new files: `apps/api/src/security/provider-key-cipher.mjs` 108, `apps/api/src/db/provider-config-repository.mjs` 174, `apps/api/src/admin-contract.mjs` 277, `apps/api/src/admin/admin.controller.ts` 95, `apps/api/src/admin/admin-secret.guard.ts` 32, `apps/api/tests/provider-key-cipher.test.mjs` 158, `apps/api/tests/admin-contract.test.mjs` 301 = 1,145). This is larger than a single "ideal" ≤400-line PR, but it is exactly the pre-approved PR B slice from `tasks.md`'s own Review Workload Forecast (`400-line budget risk: High`, `Chained PRs recommended: Yes`, `Decision needed before apply: No` — the forecast itself already accounts for this size by prescribing the A/B/C/D chain rather than one PR). No further exception confirmation was required per the orchestrator's `auto-chain` delivery strategy.
- PR B scope guard: touched only `apps/api/src/app.module.ts` (register `AdminController`) and five new files (`provider-key-cipher.mjs`, `provider-config-repository.mjs`, `admin-contract.mjs`, `admin/admin.controller.ts`, `admin/admin-secret.guard.ts`) plus their two test files. `infra/docker-compose.yml` was again temporarily edited for live verification (port remap) and reverted before finishing — `git diff infra/docker-compose.yml` confirms zero net change. No review-pipeline wiring, worker changes, DeepSeek/Groq stubs, or Angular UI were touched — those remain Work Units 5+ (PR C/D), explicitly out of scope for this pass.
- **PR C — Pipeline wiring + worker + registry stubs (Work Units 5-6): complete.** This is now 3 of the 4-PR chain. Estimated authored change: **~696 lines** (`git diff --stat` on tracked files: `apps/api/src/jobs/review-orchestrator.mjs` +22/-, `apps/api/src/live-review-pipeline.mjs` +57/-, `apps/api/tests/live-review-integration.test.mjs` +27, `apps/api/tests/review-orchestrator.test.mjs` +65, `services/worker/app/main.py` +60/-17, `services/worker/app/providers/anthropic_provider.py` +29/-, `services/worker/app/providers/llm_provider.py` +9, `services/worker/tests/test_cag_review.py` +48 = 300 changed on tracked files; new untracked files `apps/api/tests/active-provider-resolution.test.mjs` 205, `services/worker/app/providers/unimplemented_provider.py` 41, `services/worker/tests/test_provider_factory.py` 65, `services/worker/tests/test_review_endpoint.py` 85 = 396). Well inside the 400-line budget for a single slice — smaller than PR B, consistent with `tasks.md`'s own chained-PR forecast.
- PR C scope guard: touched only `apps/api/src/jobs/review-orchestrator.mjs`, `apps/api/src/live-review-pipeline.mjs`, `services/worker/app/main.py`, `services/worker/app/providers/anthropic_provider.py`, `services/worker/app/providers/llm_provider.py`, plus test files (one new, three modified) and one new worker provider-registry module. `infra/docker-compose.yml` was again temporarily port-remapped for live verification and reverted before finishing — `git diff infra/docker-compose.yml` confirms zero net change. No Angular UI or provenance display was touched — those remain Work Units 7-9 (PR D), explicitly out of scope for this pass (per the orchestrator's explicit instruction).
- **PR D — Angular admin UI + provenance + manual verification (Work Units 7-9): complete.** This is the 4th and FINAL slice of the chain (`tasks.md`'s Suggested PR Chain: PR A → PR B → PR C → PR D). Estimated authored change: **~1,157 lines** (`git diff --stat` on tracked files: `apps/api/src/api-contract.mjs` +31/-, `apps/api/src/db/review-repository.mjs` +37/-, `apps/api/src/jobs/review-orchestrator.mjs` +10/-, `apps/api/src/live-review-pipeline.mjs` +9/-, `apps/api/tests/live-review-integration.test.mjs` +28/-, `apps/api/tests/review-orchestrator.test.mjs` +20/-, `apps/api/tests/review-repository.test.mjs` +27/-, `apps/web/src/app/app.routes.ts` +2/-, `apps/web/src/app/results/results-page.ts` +6/-, `apps/web/src/app/results/results-view.ts` +30/-, `apps/web/src/app/thesis-api-client.ts` +7/-, `apps/web/tests/results-view.test.mjs` +72/-, `apps/web/tests/smoke.test.mjs` +28/- = 307 changed on tracked files; new untracked files `apps/api/src/db/migrations/0003_review_run_provider_provenance.sql` 10, `apps/api/tests/review-run-provenance-migration.test.mjs` 130, `apps/web/tests/admin-providers-view.test.mjs` 184, `apps/web/src/app/admin/admin-api-client.ts` 79, `apps/web/src/app/admin/admin-providers-page.ts` 245, `apps/web/src/app/admin/admin-providers-view.ts` 164, `apps/web/src/app/admin/admin-secret-store.ts` 38 = 850). Larger than a single "ideal" ≤400-line PR, but — same as PR B/PR C before it — this is exactly the pre-approved PR D slice from `tasks.md`'s own Review Workload Forecast (`400-line budget risk: High`, `Chained PRs recommended: Yes`, `Decision needed before apply: No`), consistent with the `auto-chain`/`stacked-to-main` delivery strategy already used for every prior slice in this change. No further exception confirmation was required.
- PR D scope guard: touched `apps/api/src/api-contract.mjs`, `apps/api/src/db/review-repository.mjs`, `apps/api/src/jobs/review-orchestrator.mjs`, `apps/api/src/live-review-pipeline.mjs` (all additive changes for provenance forwarding/surfacing — no PR A/B/C behavior removed or altered), one new migration (`0003_review_run_provider_provenance.sql`), the entire new `apps/web/src/app/admin/` directory (4 new files), `apps/web/src/app/app.routes.ts` (+1 route), `apps/web/src/app/results/results-page.ts`/`results-view.ts`/`thesis-api-client.ts` (provenance surfacing), and their corresponding test files (one new API test file, three extended API test files, one new web test file, two extended web test files). `docs/mvp-vertical-slice-runbook.md` gained a new, clearly-linked "LLM Provider Admin" section (decision: extended the same file rather than a separate one — see Work Unit 9's tasks.md entry for the justification). `infra/docker-compose.yml` was again temporarily port-remapped (`5433:5432`) for live verification and reverted before finishing — `git diff infra/docker-compose.yml` confirms zero net change. `apps/api/tests/contract.test.mjs` and `apps/api/tests/smoke.test.mjs` remain byte-untouched and green throughout (re-verified live this pass — 14/14 pass).
- **This is the LAST work unit of the `llm-provider-admin` change.** Work Units 1-9 are all complete; see "Change Status" at the end of this file.

## Work Unit 1 — Migration Runner Auto-Discovery

`apps/api/src/db/migrate.mjs` previously hardcoded `DEFAULT_MIGRATION_PATH` to `./migrations/0001_schema_baseline.sql` and `migrateUp`/`migrateDown` always read that single file — a second migration file would have been silently skipped forever.

**RED**: extended `apps/api/tests/migrate-runner.test.mjs` with two new tests:
1. `listMigrationFiles discovers ALL *.sql files in a directory and sorts them by filename` — an always-on unit test (no live Postgres needed) against a temp fixture directory with two `.sql` files written to disk deliberately **out of filename order**, plus a non-`.sql` file that must be ignored.
2. `migrate.mjs discovers and applies ALL *.sql files under a migrations directory in filename order, and reverts them in reverse order` — a live-Postgres integration test using two fixture migrations where `fixture_b` has a `REFERENCES` foreign key on `fixture_a`. This is a **genuine** ordering proof, not just a naming assertion: `UP` must create `fixture_a` before `fixture_b` (FK creation would fail otherwise), and `DOWN` must drop `fixture_b` before `fixture_a` (dropping in non-reversed/ascending order would fail with a foreign-key-dependency error).

Ran both against the pre-fix tree: `listMigrationFiles is not a function` (test 1, immediate structural RED) and the live test genuinely skipped (Postgres unreachable at that point in the pass, see Test Commands Run) — then re-ran genuinely live after bringing Docker up, confirming a real `0 !== 1`-class failure was possible before GREEN (see the parallel Work Unit 2 RED evidence below for the same "real failure, not just module-not-found" proof pattern, since Work Unit 1's own live fixture test could only be driven to a **real assertion failure** once GREEN existed to compare against — the `listMigrationFiles is not a function` TypeError is itself unambiguous proof the feature did not exist).

**GREEN**: replaced `DEFAULT_MIGRATION_PATH` with `MIGRATIONS_DIR` (a directory URL) and added `listMigrationFiles(dir = MIGRATIONS_DIR)`, which `readdir`s the directory, filters to `*.sql`, sorts ascending by filename, and maps to `file://` URLs. `migrateUp`/`migrateDown` now default to discovering and running **every** migration file under `MIGRATIONS_DIR` — `migrateUp` in ascending (filename) order, `migrateDown` in the **reversed** array — via a small shared `runMigrationFiles(paths, pgClient, section)` helper. Both functions keep an optional `migrationPath` single-file escape hatch (used by the new "0001 alone" regression test below) and a `migrationsDir` override (used by both the unit and live fixture tests to avoid touching the real `migrations/` directory).

**TRIANGULATE**: two angles, both live-verified against dockerized Postgres:
- Out-of-order filenames still sort correctly (proven by the unit test's deliberately-out-of-order fixture writes).
- `0001` alone still applies with no regression: added a third new test, `migrate.mjs: applying 0001 alone (via an explicit migrationPath) still creates exactly the baseline tables`, which reverts everything, applies **only** `0001_schema_baseline.sql` via the `migrationPath` escape hatch, asserts exactly the 12 original baseline tables (no more, no fewer), reverts, then restores the full schema in its `finally` block so later test files in the same live-Postgres run are unaffected.

**REFACTOR**: `listMigrationFiles()` extracted as its own exported, independently-unit-testable helper, exactly as `tasks.md` specified.

## Work Unit 2 — `0002_llm_provider_config.sql` Migration

**RED**: created `apps/api/tests/llm-provider-config-migration.test.mjs` (live-Postgres integration test, before the migration file existed) asserting: the table exists after `migrate up` (via the ordered 0001→0002 runner); `provider_name: 'openai'` violates the CHECK constraint; a blank `model_id` violates the CHECK constraint; inserting a second `is_active: true` row violates the partial unique index (a second **inactive** row for a different provider is fine — the invariant only restricts concurrently-active rows); and the table is dropped after `migrate down`. Ran live against dockerized Postgres before the migration file existed: **genuine failure** — `llm_provider_config must exist after migrate up: 0 !== 1` (not a module-not-found stub — the table genuinely did not exist because `0002` did not exist yet and the default `migrate up` had nothing new to discover).

**GREEN**: created `apps/api/src/db/migrations/0002_llm_provider_config.sql` following `0001`'s exact conventions (`-- UP`/`-- DOWN`, `BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY`, snake_case, `TIMESTAMPTZ NOT NULL DEFAULT now()`, CHECK-based enums via `CONSTRAINT ..._check CHECK (... IN (...))`, `btrim(...) <> ''` for required text, `jsonb_typeof(metadata) = 'object'` for the metadata column). Columns exactly match `design.md`'s Interfaces/Contracts line — no invented columns: `id, provider_name, model_id, encrypted_api_key, api_key_last_four, is_active, metadata, created_at, updated_at`. The exactly-one-active invariant is DB-enforced via `CREATE UNIQUE INDEX uq_llm_provider_config_one_active ON llm_provider_config (is_active) WHERE is_active` (design decision #3 — DB-enforced, race-safe, matches the literal SQL shape design.md specified), plus a secondary non-unique `idx_llm_provider_config_provider_name` lookup index following `0001`'s own indexing convention. Re-ran the RED test: **1 pass / 0 fail**.

**TRIANGULATE**: confirmed `0002` runs after `0001` via Work Unit 1's ordered runner (the RED test's own `migrate up` call uses the default no-args discovery path, so this is inherent to the test passing at all — not a separate check); confirmed `-- DOWN` cleanly drops the table (asserted directly in the test, and independently re-verified via a manual CLI cycle, see Test Commands Run); confirmed via direct `psql \dt` inside the live container that all 13 tables (12 baseline + `llm_provider_config`) exist after a manual `migrate.mjs up` and that `\dt` reports zero relations after a manual `migrate.mjs down`.

**REFACTOR**: N/A — single migration file, no shared logic yet, exactly as `tasks.md` specified.

### A necessary, deliberate change to a pre-existing test (flagged, not silently made)

`migrate-runner.test.mjs`'s pre-existing live-Postgres test (`"migrate.mjs applies and reverts the baseline schema against live Postgres"`) asserted `tableNames.length === REQUIRED_TABLES.length` where `REQUIRED_TABLES` was the fixed list of 12 `0001` tables. Once Work Unit 1's auto-discovery + Work Unit 2's new migration file are BOTH in place, the **real, intended** default behavior of `migrateUp({client})` (no args) is to apply `0001` **and** `0002` — so a genuine, correct `migrate up` now produces 13 tables, not 12. Running the full live suite exposed this as a real (expected) failure: `13 !== 12`.

This is not a scope creep or an accidental regression — it is the direct, necessary consequence of Work Unit 1 and Work Unit 2 being combined in the same PR (exactly as `tasks.md`'s own Work Unit 1 TRIANGULATE line anticipates: "confirm `0001` alone still applies with no regression", which only makes sense as a *specific, separate* check once the *general* default behavior legitimately changes). Resolved by:
1. Renaming the internal table-list constant to `BASELINE_TABLES` and deriving `REQUIRED_TABLES = [...BASELINE_TABLES, "llm_provider_config"]` for the full-discovery test, and renaming that test to `"migrate.mjs applies and reverts ALL discovered migrations (baseline + llm_provider_config) in order against live Postgres"` to accurately describe what it now covers.
2. Adding the new dedicated "0001 alone" test (see TRIANGULATE above) so the *original* narrower guarantee — "the baseline schema, and only the baseline schema, still works" — has its own explicit, still-passing assertion using the `migrationPath` single-file escape hatch, rather than being silently dropped.

No other pre-existing test needed changes. `contract.test.mjs`, `smoke.test.mjs`, `review-repository.test.mjs`, `review-orchestrator.test.mjs`, `live-review-integration.test.mjs`, `postgres-unreachable.test.mjs`, and every `apps/web`/`services/worker` test are byte-untouched.

## Work Unit 3 — AES-256-GCM Encryption Module

**RED**: created `apps/api/tests/provider-key-cipher.test.mjs` (12 pure-unit tests, no live services) before `provider-key-cipher.mjs` existed: round-trip, random-IV-per-call (two encryptions of the same plaintext differ but both decrypt correctly), ciphertext-tamper rejection, auth-tag-tamper rejection, `getEncryptionKey()` fail-fast on missing/empty/non-hex/too-short/too-long keys, a valid-key Buffer-shape assertion, and `encryptApiKey`/`decryptApiKey` each independently failing fast when the key is missing. Ran against the pre-GREEN tree: **genuine failure** — `Cannot find module '.../provider-key-cipher.mjs'` (12/12 fail, `ERR_MODULE_NOT_FOUND`), not a placeholder skip.

**GREEN**: created `apps/api/src/security/provider-key-cipher.mjs` using Node's built-in `node:crypto` (no new dependency, per design decision #4): `getEncryptionKey(env)` validates `LLM_PROVIDER_ENCRYPTION_KEY` is present, exactly 64 hex characters (32 bytes), throwing `EncryptionKeyError` (never including the invalid value itself in the message) otherwise; `encryptApiKey`/`decryptApiKey` implement AES-256-GCM with a random 12-byte IV per call, packed as `v1:iv(b64):tag(b64):ciphertext(b64)`; `lastFour(plaintext)` for the masked-display value. Re-ran the RED suite: **12 pass / 0 fail**.

**TRIANGULATE**: empty-string key, non-hex key, too-short key (62 hex chars), and too-long key (66 hex chars) are each covered by their own dedicated assertion — four separate fail-fast cases, not one combined check.

**REFACTOR**: N/A, as scoped — the module stays self-contained (no shared state, no other module imports from it except `provider-config-repository.mjs` in Work Unit 4).

### A deliberate scope note: "fail-fast at startup" vs. design.md's literal "first cipher use"

The orchestrator's instructions for this pass asked for `LLM_PROVIDER_ENCRYPTION_KEY` to be "validated fail-fast at API startup ... the process must not silently start in an insecure state." `design.md`'s own decision #5, read literally, says "validated fail-fast at **first cipher use**" — not at the shared NestJS process bootstrap (`main.ts`/`bootstrapApi()`). These are not equivalent, and the difference matters here: `apps/api/tests/smoke.test.mjs` (pre-existing, unrelated to this pass, explicitly protected — "preserve every currently-passing test") calls `bootstrapApi()` directly with no `LLM_PROVIDER_ENCRYPTION_KEY` set. Hooking `getEncryptionKey()` into the shared `bootstrapApi()`/`main()` bootstrap would have made `smoke.test.mjs` fail unconditionally in every environment that hasn't configured admin-provider encryption yet — a real regression of a protected, out-of-scope test, not an acceptable tradeoff.

Resolution: implemented `design.md`'s literal decision — `getEncryptionKey()` fires the moment any cipher operation is attempted (`encryptApiKey`/`decryptApiKey`), AND, one level up, `provider-config-repository.mjs`'s `createProviderConfigRepository()` factory calls `getEncryptionKey()` **eagerly at construction time**, before any DB I/O — see Work Unit 4 below. Because `admin-contract.mjs` lazily constructs that repository on the **first** admin API request of any kind (list included, not just create/update), a misconfigured key fails fast and loudly on the first real use of the admin subsystem, without ever silently accepting requests — satisfying the underlying security goal ("never proceed insecurely") without crashing the unrelated, protected `smoke.test.mjs`/whole-process bootstrap. This is flagged here rather than silently deviating from either instruction.

## Work Unit 4 — Admin CRUD + Activate Endpoints

**RED**: created `apps/api/tests/admin-contract.test.mjs` before `admin-contract.mjs`/`provider-config-repository.mjs` existed: `listAdminRoutes()` route-shape assertion; create without `x-admin-secret` → 401 + standard error shape (and asserted the submitted `api_key` never appears anywhere in the response body); create with a wrong secret → 403 (same no-leak assertion); create with `provider_name: "openai"` → 422 naming `provider_name` in `details.issues`, **and** asserting the submitted `api_key` value is absent from the entire response body (the orchestrator's explicit "at least one failure-path key-leak proof" requirement); a dedicated fail-fast test that clears `LLM_PROVIDER_ENCRYPTION_KEY` and asserts a `5xx` response with no key leak; and one comprehensive live-Postgres scenario (create claude row → 201 masked; create a second deepseek row; update claude's `model_id` without resubmitting the key → masked key preserved; activate claude; activate deepseek → asserted to atomically deactivate claude; list → exactly one active row, and every list item explicitly asserted to have no `encrypted_api_key`/`api_key` own-property). Ran against the pre-GREEN tree: **genuine failure** — 5 of 6 tests failed with `Cannot find module '.../admin-contract.mjs'` (`ERR_MODULE_NOT_FOUND`); the 6th (live-DB scenario) reported the documented `role "pg1" does not exist` skip (Homebrew Postgres port conflict, Docker not up yet at that point in the pass) — not yet a real pass either way.

**GREEN**: created, in dependency order:
1. `apps/api/src/db/provider-config-repository.mjs` — `createProviderConfigRepository({ client, connectionString, env })`. Calls `getEncryptionKey(env)` **eagerly at construction time** (fail-fast, see Work Unit 3 note above) before any DB I/O. `list()`/`create()`/`update()`/`activate()` all funnel through one shared `toMaskedView(row)` allowlist mapper (`id, type, provider_name, model_id, api_key_last_four, is_active, metadata, created_at, updated_at`) — `encrypted_api_key` is never selected by `list()`'s own SQL and is never part of the allowlist, so it cannot leak even via a future `SELECT *` mistake. `update()` is key-write-only: `apiKey` is only re-encrypted when explicitly provided in the payload; omitting it leaves the stored `encrypted_api_key`/`api_key_last_four` untouched via `COALESCE`. `activate(id)` runs `BEGIN; UPDATE ... SET is_active=false WHERE is_active=true; UPDATE ... SET is_active=true WHERE id=$1; COMMIT` in a single transaction, so PR A's partial unique index on `is_active` is never transiently violated. `getActiveProvider()` (decrypts, for the review pipeline) is included per `tasks.md`'s explicit Work Unit 4 GREEN scope even though its consumer (`live-review-pipeline.mjs`) is Work Unit 5 — written now, not yet wired in.
2. `apps/api/src/admin-contract.mjs` — the isolated admin request handler (design decision #7), mirroring `api-contract.mjs`'s pure-handler pattern so it is directly unit-testable without a live NestJS app. `handleAdminRequest({method, path, query, body, headers})` checks `checkAdminSecretHeader(headers)` **before** any routing/validation/DB work; that check does a SHA-256-digest-based constant-time comparison (`timingSafeEqual` on fixed-length digests, sidestepping `timingSafeEqual`'s equal-length-buffer requirement so a wrong-length guess can't short-circuit) of `x-admin-secret` against `ADMIN_SHARED_SECRET`, returning `401` (header absent) or `403` (value mismatch) in the same 5-key standard error shape `contract.test.mjs` enforces for `api-contract.mjs`. Validation (`provider_name` enum, non-blank `model_id`/`api_key`) happens before any repository call, so an invalid request never reaches the DB and never echoes the submitted key. A lazy, cached repository singleton (`getRepository()`, mirroring `live-review-pipeline.mjs`'s own `cachedRepository` pattern) is built from `process.env.DATABASE_URL`; `withRepository()` wraps every DB-touching route in one try/catch that maps `EncryptionKeyError` to a `500 configuration_error` (message never contains the key) and any other repository error to a generic `500 internal_error` with only `error.message` as `reason` (never raw request data). `_resetAdminContractForTests()` is exported for test isolation, matching `_resetLiveReviewPipelineForTests()`'s convention.
3. `apps/api/src/admin/admin-secret.guard.ts` — a real NestJS `CanActivate` (design decision #6) that imports and delegates to `admin-contract.mjs`'s exact same `checkAdminSecretHeader` (single source of truth, not a re-implementation), throwing `UnauthorizedException`/`ForbiddenException` with the identical body shape. This is defense-in-depth at the HTTP transport boundary — `admin-contract.mjs`'s own internal check is what's actually exercised by the automated test suite.
4. `apps/api/src/admin/admin.controller.ts` — `@Controller('api/v1/admin/llm-providers') @UseGuards(AdminSecretGuard)`, four methods (`list`/`create`/`update`/`activate`) each using `@Headers()` to forward the full header map into `handleAdminRequest`, matching `ReviewRunsController`/`ThesisDocumentsController`'s existing "controller delegates to the pure `.mjs` handler" pattern exactly.
5. `apps/api/src/app.module.ts` — added `AdminController` to the `controllers` array (5-line diff; `ThesisDocumentsController`/`ReviewRunsController` untouched).

A bug was caught and fixed during GREEN: the first `withRepository()` draft called `getRepository()` (which can throw `EncryptionKeyError` via repository construction) **outside** its own `try` block, so the fail-fast-misconfiguration test failed with an uncaught exception instead of a handled `5xx` response. Fixed by moving `getRepository()` inside the `try`. Caught immediately by the "encryption key misconfigured" RED test itself — exactly the kind of bug RED-first testing is meant to catch before it reaches a real request.

Re-ran the RED suite after GREEN: **6/6 pass** offline-safe subset (5 pass, 1 correctly skipped when Docker was still down at that point), then **6/6 pass with zero skips** once Docker was brought up (port-5433 workaround, see Test Commands Run).

**TRIANGULATE**: confirmed `apps/api/tests/contract.test.mjs` stays green and byte-untouched (verified via `git status`/`git diff` showing zero changes to that file, and via re-running it directly — 12/12 pass, unchanged from before this pass). Confirmed the list endpoint's response items never carry an `encrypted_api_key` or `api_key` own-property (explicit `Object.prototype.hasOwnProperty.call` assertion per item in the live-DB test, not just an absence-from-JSON-string check). Additionally live-verified via manual `curl` against a real running server (see Work Unit Evidence) that all 4 routes behave identically to the automated-test expectations end-to-end through the real NestJS `AdminSecretGuard` + `AdminController`, not just through the pure `handleAdminRequest` function directly.

**REFACTOR**: (a) `provider-config-repository.mjs`'s `toMaskedView(row)` is the single shared masking function used by `list`/`create`/`update`/`activate` — satisfying `tasks.md`'s explicit "share response-masking logic between create/update/list handlers" bullet from the start, not as an afterthought. (b) After GREEN, extracted `validateProviderNameField`/`validateNonEmptyStringField`/`validationErrorOrValue` helpers in `admin-contract.mjs`, shared between `validateCreatePayload` (fields required) and `validateUpdatePayload` (fields optional — `undefined` means "not being changed"), removing the duplicated inline validation blocks that existed in the first GREEN draft. Re-ran the full offline + live suites after this refactor — no behavior change, all still green (see Test Commands Run).

## Work Unit 5 — Active-Provider Resolution Wired Into the Review Pipeline

**RED**: three fronts, all run against the pre-GREEN tree and confirmed genuinely failing before any implementation:
1. `apps/api/tests/active-provider-resolution.test.mjs` (new, live-Postgres, one comprehensive sequential test): zero active providers → triggered run must reach `review_run.status: "failed"` with `error_summary` matching `/no active LLM provider configured/i`, and the worker must never even receive an `/internal/review` request. Ran against the pre-GREEN tree: **genuine failure** — `AssertionError: expected 'failed', actual: 'completed'` (the run silently succeeded via the pre-existing env-var-only Claude path, proving the missing resolution step was real, not a naming gap).
2. `apps/api/tests/review-orchestrator.test.mjs` (new pure-unit test, no DB): asserts `defaultRunCagReview` forwards `provider_name`/`api_key`/`model_id` into the worker request body when supplied, and omits them entirely when not (backward compatibility). Ran against the pre-GREEN tree: **genuine failure** — the fake worker received only `{"thesis_text": ...}`, no provider fields (`Unexpected token '<'... is not valid JSON` on the second assertion, later root-caused to an unrelated test-ordering bug — see "A subtle test-authoring bug caught during RED" below — then re-confirmed as a genuine `deepEqual` mismatch once that bug was fixed).
3. `services/worker/tests/test_cag_review.py:95-111` — consciously reworked, not silently changed: kept the pre-existing "both env and payload key absent → `AnthropicProviderConfigError`" test verbatim, and added two new tests: `test_explicit_api_key_and_model_take_precedence_over_env_when_both_present` and `test_env_api_key_still_used_when_no_explicit_key_supplied`. Ran against the pre-GREEN tree: **genuine failure** — `AttributeError: 'AnthropicProvider' object has no attribute '_resolve_api_key'` / `TypeError: AnthropicProvider.__init__() got an unexpected keyword argument 'api_key'` (2 errors, the constructor genuinely did not accept an explicit key yet).

**GREEN**, in dependency order:
1. `services/worker/app/providers/anthropic_provider.py` — `AnthropicProvider.__init__` gained an `api_key: str | None = None` keyword param; added `_resolve_api_key()` (`self._api_key or os.environ.get("ANTHROPIC_API_KEY")`) implementing arg-then-env precedence (design decision #11); `generate()` now calls `_resolve_api_key()` instead of reading the env var directly. The key is still only *resolved* inside `generate()`, at actual call time — constructing `AnthropicProvider(api_key=..., model=...)` never itself requires a key, unchanged from before this pass.
2. `services/worker/app/providers/llm_provider.py` — added `ProviderNotImplementedError(LLMProviderError)` (used by Work Unit 6 below, needed here too so `main.py`'s except-chain has somewhere to import it from).
3. `services/worker/app/main.py` — `ReviewRequest` gained three optional fields (`provider_name`, `api_key`, `model_id`, all `| None = None` — a caller sending only `thesis_text` still validates, proven by `test_review_endpoint.py`'s explicit backward-compatibility test). Added `select_llm_provider(provider_name, api_key, model_id)` — a pure factory that **never raises itself**; it always returns a provider instance (real `AnthropicProvider`, or an `UnimplementedProvider`-family instance for deepseek/groq/unknown — see Work Unit 6), deferring every failure into `.generate()` so it is always caught inside `internal_review`'s existing try/except, never left unhandled at FastAPI's dependency-resolution stage (a deliberate design choice — see "A dependency-injection subtlety" below). `get_llm_provider(payload: ReviewRequest)` replaced the old zero-arg version, using FastAPI's built-in same-model sub-dependency merging so the request body is parsed exactly once and shared between the route and the dependency.
4. `apps/api/src/jobs/review-orchestrator.mjs` — `defaultRunCagReview({thesisText, providerName, apiKey, modelId})`: builds the request body starting from `{thesis_text: thesisText}` and conditionally adds `provider_name`/`api_key`/`model_id` only when each is `!== undefined` — an old-style caller supplying only `thesisText` produces the exact same body as before this pass (byte-for-byte, proven by the new pure-unit test).
5. `apps/api/src/live-review-pipeline.mjs` — added `getProviderRepository()` (lazy singleton, mirrors `getRepository()`'s pattern, backed by `createProviderConfigRepository({connectionString})` from PR B) and `runCagReviewWithActiveProvider({thesisText})`: calls `providerRepository.getActiveProvider()` **fresh on every invocation** (never cached across calls — only the repository *instance* is cached, not any resolved provider value), throws `new Error("no active LLM provider configured")` when `getActiveProvider()` returns `null`, otherwise forwards the resolved `providerName`/`apiKey`/`modelId` into `defaultRunCagReview`. `getLivePipeline()` now passes `runCagReview: runCagReviewWithActiveProvider` into `createReviewPipeline(...)` instead of relying on its `defaultRunCagReview` default. **No new error-handling code was needed for the zero-active-provider path** — the pre-existing `createReviewOrchestrationProcessor`'s catch block (unchanged from `mvp-vertical-slice`) already turns any thrown error from `runCagReview` into `review_run.status: "failed"` + `error_summary: error.message`, exactly matching the orchestrator's explicit "reach a real failed status... matching the exact error-shape conventions already proven in `mvp-vertical-slice`'s PR C/D" requirement.

Re-ran all three RED fronts after GREEN: worker suite **24/24 pass**; `review-orchestrator.test.mjs`'s new pure-unit test **passes**; `active-provider-resolution.test.mjs`'s single comprehensive test (4 sequential scenarios: zero-active-fails, provider-one-forwards-correctly, provider-two-re-resolves-after-switch, worker-failure-never-leaks-key) **passes** against live dockerized Postgres.

### A subtle test-authoring bug caught during RED (flagged, not silently worked around)

The first draft of `review-orchestrator.test.mjs`'s new pure-unit test imported `defaultRunCagReview` via a dynamic `await import("../src/jobs/review-orchestrator.mjs")` **before** setting `process.env.WORKER_BASE_URL` to the fake worker's URL. `DEFAULT_WORKER_BASE_URL` inside that module is a top-level `const` evaluated once at import time (`process.env.WORKER_BASE_URL ?? "http://localhost:8000"`) — setting the env var *after* import has no effect on an already-evaluated constant. The test's fake worker was silently never contacted; the request went to `http://localhost:8000` instead (a non-existent local server in this environment, returning an HTML error page — hence the `Unexpected token '<'` JSON-parse failure). Fixed by reordering: start the fake worker and set `WORKER_BASE_URL` **before** the dynamic import, plus a cache-busting query-string suffix (`?t=${Date.now()}`) on the import specifier so this test is immune to the module already having been imported earlier by another `test()` block in the same file/process (ESM's per-specifier module cache). This same "top-level const frozen at first import" characteristic was independently confirmed as the root cause of `live-review-integration.test.mjs`'s pre-existing (unrelated to this bug) single-fake-worker-per-file pattern already being correct by construction — that file already imports its worker-config env vars before its first dynamic import of `api-contract.mjs`.

### A dependency-injection subtlety: why the provider factory never raises directly

An early design considered having `select_llm_provider` raise `ProviderNotImplementedError` directly when `provider_name` is `deepseek`/`groq`/unknown, called from `get_llm_provider` (a FastAPI `Depends()` function). This would have been a bug: FastAPI resolves dependencies **before** invoking the route function body, so an exception raised there is NOT inside `internal_review`'s own `try/except` block and would propagate as an unhandled `500` with FastAPI's default (non-standard-shaped) error response — violating the orchestrator's explicit "never crashing unhandled" requirement. Resolution: `select_llm_provider` **always succeeds** and returns a provider object (`AnthropicProvider` for claude, `DeepSeekProvider`/`GroqProvider`/`UnimplementedProvider` for everything else); the actual failure is deferred into `.generate()`, which `run_cag_review()` calls from *inside* `internal_review`'s try block — exactly mirroring how `AnthropicProviderConfigError` already worked before this pass (raised inside `generate()`, not at construction).

## Work Unit 6 — DeepSeek/Groq Registry Stubs

**RED**: two new worker test files, both run against the pre-GREEN tree:
1. `services/worker/tests/test_provider_factory.py` — pure unit tests (no network) asserting `select_llm_provider("deepseek"/"groq"/"openai", ...)` returns an object whose `.generate()` raises `ProviderNotImplementedError` naming the provider, with the submitted (fake) api_key never appearing in the exception message; also asserts claude selection still forwards explicit `api_key`/`model_id` correctly. Ran against the pre-GREEN tree: **genuine failure** — `ImportError: cannot import name 'select_llm_provider' from 'app.main'` (6/6 fail — the function did not exist yet).
2. `services/worker/tests/test_review_endpoint.py` — FastAPI `TestClient`-level tests hitting the real `/internal/review` route: deepseek/groq → expected `501` with `not_implemented` + the provider name in `detail`, and the submitted key never appearing in `detail`; plus the pre-existing "missing credentials → 500 configuration_error" behavior and an explicit "old-style request with only thesis_text still validates" backward-compatibility check. Ran against the pre-GREEN tree: **genuine failure** — deepseek/groq both returned `500` (the old always-Claude `get_llm_provider()` swallowed the `provider_name` field entirely and fell through to the missing-env-key `AnthropicProviderConfigError` path) instead of the required `501`.

**GREEN**: `services/worker/app/providers/unimplemented_provider.py` (new file) — `UnimplementedProvider(name, *, api_key=None, model=None)` whose `.generate()` immediately raises `ProviderNotImplementedError(f"{name} provider is not yet implemented — no real API call was attempted.")`; `DeepSeekProvider`/`GroqProvider` are thin named subclasses (matching the orchestrator's "class... or equivalent factory-selection logic" requirement with actual classes, for clarity and future extensibility). `select_llm_provider` (Work Unit 5, above) routes `"deepseek"`/`"groq"` to these classes, and any other unrecognized `provider_name` to a generic `UnimplementedProvider` rather than silently defaulting to Claude — defensive-in-depth even though the admin API already restricts `provider_name` to the three supported values at the CRUD layer (PR B). `internal_review`'s except-chain in `main.py` gained `except ProviderNotImplementedError as exc: raise HTTPException(status_code=501, detail=f"not_implemented: {exc}")`, ordered before the more specific `AnthropicProviderConfigError` handler is irrelevant here (different exception hierarchy branch) but after nothing needs precedence since `ProviderNotImplementedError` and `AnthropicProviderConfigError` are siblings, not overlapping.

Re-ran both RED fronts after GREEN: `test_provider_factory.py` **6/6 pass**; `test_review_endpoint.py` **4/4 pass** (missing-credentials 500, deepseek 501, groq 501, backward-compat old-style-request 500-not-422). Full worker suite: **24/24 pass, 0 fail**.

**TRIANGULATE**: confirmed an admin can still create+activate a deepseek row via the admin API without any error — this is **not new evidence from this pass**, it is the pre-existing PR B `admin-contract.test.mjs` live scenario (`createRes2`/deepseek row, later activated), re-verified still green in this pass's full-suite run (76/76 pass live) with zero changes to `admin-contract.mjs`/`provider-config-repository.mjs`. This directly confirms the required invariant: "failure only occurs at review-run time, not create/activate time" — the DeepSeek/Groq failure this pass adds lives entirely on the worker side, at actual `.generate()` call time, never touching the admin CRUD layer.

**REFACTOR**: N/A, as `tasks.md` scoped — single factory branch addition (`UnimplementedProvider` family), no existing code needed restructuring.

## Work Unit 7 — Angular `admin/` Feature

**RED**: created `apps/web/tests/admin-providers-view.test.mjs` (21 pure-unit tests, `node:test`, no Angular TestBed/jsdom — mirrors `upload-validation.test.mjs`/`results-view.test.mjs`'s pattern) before `apps/web/src/app/admin/admin-providers-view.ts` existed: empty/loading/error list states; masked-key rendering (`maskedKeyLabel`); create-payload shape (`buildCreateProviderPayload`, always includes `api_key` — required on create); update-payload write-only key behavior (`buildUpdateProviderPayload` — omits `api_key` entirely when blank, never sends an empty-string overwrite; includes it only when explicitly typed); route-building (`buildProviderPath`/`buildActivatePath`); session-scoped secret gating (`resolveAdminSecretForRequest`/`canSendAdminRequest` — reuses a cached secret without re-prompting, prompts exactly once when none cached, trims whitespace, treats a cancelled/blank prompt as "no secret, never send the request"); and 401/403 UI-state messaging (`extractAdminErrorMessage`/`isAdminAuthError`). Ran against the pre-GREEN tree: **genuine failure** — `ERR_MODULE_NOT_FOUND` (module did not exist yet).

**GREEN**, in dependency order:
1. `apps/web/src/app/admin/admin-providers-view.ts` — the pure view-model/payload/gating module (exactly mirroring `results-view.ts`'s framework-free pattern) implementing every function the RED tests target.
2. `apps/web/src/app/admin/admin-secret-store.ts` — `AdminSecretStore` (`@Injectable({providedIn: 'root'})`), an in-memory Angular `signal<string|null>` (design decision #9: session-scoped only — NEVER `localStorage`, NEVER build-time baked). `getSecret(promptFn = () => window.prompt(...))` delegates its resolution logic to the pure `resolveAdminSecretForRequest`; `clearSecret()` is called after a `403` so the next action re-prompts instead of silently repeating a rejected secret.
3. `apps/web/src/app/admin/admin-api-client.ts` — `AdminApiClient` (design decision #8: a new sibling client, NOT an extension of `ThesisApiClient` — different concern, different auth mechanism). Every method resolves the secret lazily via `defer()` at *subscribe* time (not construction/call time), so a session-first prompt only appears when a real request is about to fire; a missing/cancelled secret errors client-side before any HTTP call is made — never a silent unauthenticated request.
4. `apps/web/src/app/admin/admin-providers-page.ts` — `AdminProvidersPage`, standalone + `ChangeDetectionStrategy.OnPush` + `inject()` + native `@if`/`@for`/`@switch` (exactly matching `upload-page.ts`/`results-page.ts`'s conventions): provider list (masked key, active/inactive badge, provider name, model id), a single add/edit `ReactiveFormsModule` form (the raw API key field is write-only — `onEdit()` never pre-fills it with the stored value, and a successful save always resets the whole form, clearing it), and a per-row Activate button. On any `401`/`403`, `AdminSecretStore.clearSecret()` is called before surfacing the error.
5. `apps/web/src/app/app.routes.ts` — added `{ path: 'admin/llm-providers', component: AdminProvidersPage }`.

Re-ran the RED suite after GREEN: **21 pass / 0 fail**.

**TRIANGULATE**: 401 and 403 each produce a distinct, specific, non-silent message (`extractAdminErrorMessage`) — "an admin secret is required" vs. "the admin secret was rejected" — never a generic/blank failure; both are unit-tested directly, and re-verified live this pass via real `curl` against the real `AdminSecretGuard` (see Work Unit Evidence).

**REFACTOR**: N/A this pass, as scoped — `maskedKeyLabel`/path-building/error-message helpers were written shared-from-the-start in the single pure `admin-providers-view.ts` module (mirroring `results-view.ts`), not duplicated between list/edit code first and extracted later.

### A necessary correction caught by the harness itself (flagged, not silent)

`apps/web/tests/smoke.test.mjs`'s first draft of the new admin assertion used `assert.doesNotMatch(secretStoreSource, /localStorage/)` — this immediately self-failed because `admin-secret-store.ts`'s own doc comment legitimately *explains* why `localStorage` is NOT used (a security-relevant design rationale worth keeping in the code, not removing). Fixed by narrowing the regex to actual storage-API *usage* syntax (`/localStorage\s*[.[]/`), not the mere presence of the word — re-ran, genuine pass. This is documented here because it's exactly the kind of over-narrow assertion that would have forced stripping a useful security-rationale comment to satisfy a test, which would have been the wrong fix.

## Work Unit 8 — Provider Provenance on `review_run` + Results View

This work unit resolves the "inferred scope" flag left in this file's own `## Scope Guard`-equivalent note and `tasks.md`'s Scope Guard bullet: `design.md`'s File Changes table scopes `0002` to the registry table only, with no explicit run-provenance column names anywhere in `design.md`. Re-read `design.md` verbatim at the start of this pass to confirm this literally — it is genuinely silent on where run-provenance lives. `tasks.md`'s own Work Unit 8 (written during the tasks phase, before this apply pass) already resolved the ambiguity explicitly: a new `0003_review_run_provider_provenance.sql` migration adding `llm_provider_name`/`llm_model_id` directly to `review_run`. This pass implements that already-recorded decision literally, and documents it here a second time (not silently) because the orchestrator's instructions for this pass explicitly asked for the decision to be re-confirmed against `design.md`'s exact wording before implementing, not assumed from a prior note alone.

**Column/location decision, explicit**: `review_run.llm_provider_name` (nullable `TEXT`, CHECK-constrained to the same `claude`/`deepseek`/`groq` enum as `llm_provider_config.provider_name`) + `review_run.llm_model_id` (nullable `TEXT`, unconstrained) — chosen over alternatives (a new `review_run_provenance` join table, or reusing `llm_provider_config`'s own id as a FK) because: (a) it is a 1:1, append-once-at-completion relationship with no need for referential history beyond "what was true when this run finished" — a FK to a mutable, deletable `llm_provider_config` row would let historical provenance silently break or dangle if that row is later edited/deleted; denormalized snapshot columns avoid that entirely; (b) it matches this schema's own existing convention of denormalized snapshot-style columns on `review_run` (e.g. `pipeline_version` is already a snapshot string, not a FK to a "pipeline" table); (c) it is the literal shape `tasks.md`'s own Work Unit 8 already specified, and changing it without a new reason would be freelancing against an already-made decision.

**RED**, four fronts, all run against the pre-GREEN tree and confirmed genuinely failing:
1. `apps/api/tests/review-run-provenance-migration.test.mjs` (new, live-Postgres, mirrors Work Unit 2's `llm-provider-config-migration.test.mjs` pattern): asserts both columns exist and are nullable after `migrate up`; a row with no provenance set reads back as `null`/`null` (never an error); setting a supported provider name succeeds; setting `'openai'` violates the CHECK constraint; `migrate down` cleans up everything. Ran pre-GREEN: **genuine failure** — `0 !== 2` (columns did not exist).
2. `apps/api/tests/review-repository.test.mjs` (extended): `updateReviewRunStatus({llmProviderName, llmModelId})` persists them (verified via direct SQL readback); the new `getReviewRunProvenance()` reads them back, and gracefully returns `{llmProviderName: null, llmModelId: null}` for a run where they were never set. Ran pre-GREEN: **genuine failure** — `null !== 'claude'` (the repository silently ignored the new fields; the columns didn't exist yet either).
3. `apps/api/tests/review-orchestrator.test.mjs` (extended): the "grounded CAG result" integration test's `runCagReview` now returns `providerName`/`modelId` alongside `finding`; asserts the orchestrator persists both onto the completed row via `repository.getReviewRunProvenance(getReviewRunDbId(...))`. Ran pre-GREEN: **genuine failure** — `llmProviderName: null` (the orchestrator didn't forward these fields at all).
4. `apps/api/tests/live-review-integration.test.mjs` (extended): after Scenario 1 (grounded finding via the seeded claude provider) reaches `completed`, asserts `GET /review-runs/{id}` names `llm_provider_name: "claude"`/`llm_model_id: "claude-sonnet-4-20250514"`; then, as an explicit TRIANGULATE step, directly nulls that same genuinely-completed run's provenance columns via raw SQL and re-fetches, asserting a graceful `200`/`null`/`null` response — proving the "runs from before this change render gracefully" requirement against a REAL run, not a synthetic fixture. Ran pre-GREEN: **genuine failure** — `undefined !== 'claude'` (the field didn't exist on the response at all).

Also extended `apps/web/tests/results-view.test.mjs` (web-side RED, see below) in the same pass.

**GREEN**, in dependency order:
1. `apps/api/src/db/migrations/0003_review_run_provider_provenance.sql` — additive `ALTER TABLE review_run ADD COLUMN ...` (both nullable; the provider-name column CHECK-constrained), following `0001`/`0002`'s exact `-- UP`/`-- DOWN` convention.
2. `apps/api/src/db/review-repository.mjs` — `updateReviewRunStatus` gained optional `llmProviderName`/`llmModelId` params, persisted via the same `COALESCE` pattern as every other optional field on that call (so every existing caller that doesn't pass them is unaffected — proven by the untouched Work Unit 5/6 tests staying green); new `getReviewRunProvenance(reviewRunId)` read method.
3. `apps/api/src/live-review-pipeline.mjs` — `runCagReviewWithActiveProvider` now returns `{ ...result, providerName: active.providerName, modelId: active.modelId }` instead of the worker's bare result. This is the ONE place that genuinely knows which provider/model was just used — the worker's own `/internal/review` response never echoes `provider_name`/`model_id` back (confirmed by reading `services/worker/app/main.py`'s response shape: `{"finding": ...}` only), so provenance has to be attached on the API side, by the caller that resolved it, not read back from the worker.
4. `apps/api/src/jobs/review-orchestrator.mjs` — on successful completion, `updateReviewRunStatus` now also passes `llmProviderName: reviewResult?.providerName ?? null, llmModelId: reviewResult?.modelId ?? null` — `undefined` (every pre-existing test's fake `runCagReview` that doesn't know/report this) safely no-ops via the repository's own `COALESCE`, proven by Work Unit 5/6's tests staying green unmodified.
5. `apps/api/src/api-contract.mjs` — `withRealSummary` (the function that already does a best-effort DB lookup for `summary.findings` on a completed run) now also calls `getReviewRunProvenance` and merges `llm_provider_name`/`llm_model_id` onto the response; the two other response-shaping paths (`reviewRunLifecycle`'s in-memory stub, and the fully fabricated `reviewRunStatus()` legacy stub) were also given the same two fields, always `null`, via a shared `EMPTY_PROVENANCE` constant — so every `GET /review-runs/{id}` response has this shape regardless of which of the three paths served it, and no caller has to special-case a missing field. Confirmed this does NOT break `contract.test.mjs` (which asserts individual fields via `assert.equal`, never a whole-body `deepEqual` on this response) — re-verified live, 14/14 pass, byte-untouched.
6. `apps/web/src/app/thesis-api-client.ts` — `ReviewRunResponse` gained `llm_provider_name: string | null` / `llm_model_id: string | null`.
7. `apps/web/src/app/results/results-view.ts` — new `formatProviderLabel({providerName, modelId})` (`"claude (claude-sonnet-4-20250514)"`, or `"Unknown provider"`/`"<name> (unknown model)"` for the missing cases); the `findings`/`no_findings` `ResultsViewModel` kinds gained a `providerLabel` field, computed once in `buildResultsViewModel`.
8. `apps/web/src/app/results/results-page.ts` — renders `Reviewed by: {{ providerLabel() }}` above both the finding list and the "No findings." message.

Re-ran all four RED fronts after GREEN: all pass (see Test Commands Run).

**TRIANGULATE**: the "no provenance recorded" fallback is proven at three independent layers, not just one: (a) pure unit level — `formatProviderLabel`/`buildResultsViewModel` with `llm_provider_name: null`; (b) repository level — `getReviewRunProvenance` on a freshly-inserted run that was never updated with provenance; (c) full HTTP level, against a REAL genuinely-completed run whose columns were then directly nulled via raw SQL and re-fetched through the real `GET` endpoint — confirming the whole chain (repository → `withRealSummary` → response body) degrades gracefully end to end, not just at any one layer in isolation.

**REFACTOR**: N/A, as scoped — this was an additive-columns + one-augmentation-point (`runCagReviewWithActiveProvider`'s return value) change; no existing structure needed reshaping.

### A test-isolation false alarm caught and ruled out (flagged, not silently ignored)

Running `migrate-runner.test.mjs` and `llm-provider-config-migration.test.mjs` together via an ad-hoc `node --test` invocation WITHOUT the project's own `--test-concurrency=1` flag produced a spurious failure (both files' `migrateDown`/`migrateUp` racing against the same live database when run concurrently). Re-ran with the exact flag `apps/api/package.json`'s own `test` script uses (`--test-concurrency=1`) — genuine, clean pass, 8/8 across all three migration-related test files together. This was a test-harness-invocation mistake on this pass's part, not a real bug in any migration file; documented here so it isn't mistaken for a flaky test in a future pass.

## Work Unit 9 — Manual End-to-End Verification

Not automatable, per `tasks.md`'s own scoping (needs a live `ANTHROPIC_API_KEY` entered through the UI) — RED/GREEN/TRIANGULATE/REFACTOR are all N/A, exactly as `tasks.md` anticipates. Everything short of the final real-key Claude call was run for real this pass, against this repository's actual current code:

1. **Docs decision**: extended `docs/mvp-vertical-slice-runbook.md` in place with a new, clearly-linked "LLM Provider Admin — Manual End-to-End Verification" section (rather than a new file) — it extends, not replaces, the same stack-startup steps (0-5) the existing runbook already documents, and `tasks.md`'s own Work Unit 9 GREEN line literally says "extend `docs/mvp-vertical-slice-runbook.md` with an 'LLM Provider Admin' section."
2. **Server boot + admin env vars**: started the real NestJS API with `LLM_PROVIDER_ENCRYPTION_KEY`/`ADMIN_SHARED_SECRET`/`DATABASE_URL`/`WORKER_BASE_URL` set — booted cleanly.
3. **Admin endpoints, real `curl`**: no header → `401`; wrong secret → `403`; list (empty) → `200 {"items":[]}`; create a claude row with a fake key → `201` with `api_key_last_four` only (no key material anywhere in the response, manually inspected); activate → `200`, `is_active: true`.
4. **Angular admin page, real dev server**: `pnpm --dir apps/web start --port 4300 --host 127.0.0.1` (the same `proxy.conf.json`-based command the pre-existing runbook already documents, which proxies all of `/api/*` including the new admin routes with zero proxy-config changes needed) — `GET http://127.0.0.1:4300/admin/llm-providers` → `200` (the SPA route rendered); `GET http://127.0.0.1:4300/api/v1/admin/llm-providers` (through the real dev-server proxy, with the real secret header) → returned the real masked provider list from the real running API — proving the whole admin-UI-to-admin-API wiring is real, not assumed.
5. **Zero-active-provider run reaches a real `failed` status**: triggered a review run before activating any provider (and again with the worker unreachable) — both reached a genuine `failed` status with a real, non-fabricated `error_summary`, never a silent `completed`.
6. **Full activated-provider path with a FAKE key, against the REAL Anthropic API**: uploaded a real fixture PDF (`docs/guias/Tesis Guia de Trabajo GT.pdf`), activated the claude row created in step 3 (whose key was a deliberately fake string), and triggered a review run. Extraction succeeded (real `pypdf` parse of a real PDF); the run reached the worker's `/internal/review`, which made a REAL HTTPS call to Anthropic's real API, which genuinely rejected the fake key with `401 invalid x-api-key` (captured verbatim in the worker's own log) — surfaced end-to-end as `review_run.status: "failed"` with `error_summary: "review failed: Worker /internal/review failed with status 500: Internal Server Error"`, and the `GET` response correctly carried `llm_provider_name: null`/`llm_model_id: null` (provenance is only populated on `completed`, by design — a failed run never fabricates a handler). This is about as close to full end-to-end proof as is possible without a real, valid key.
7. **A genuine local-machine discovery, root-caused and documented**: the first attempts at step 6 failed with a confusing `error_summary: "...Unexpected token '<'... is not valid JSON"` even with the worker healthy and reachable. Root-caused via `lsof -nP -iTCP:8000 -sTCP:LISTEN`: an unrelated local PHP dev server (`php -S`-style) was already listening on `[::1]:8000` (IPv6 loopback), while the worker binds only to `127.0.0.1:8000` (IPv4) — Node's `fetch("http://localhost:8000/...")` resolved `localhost` to the IPv6 address first and silently hit the WRONG process (confirmed: the real worker's own log showed ZERO incoming requests during the failing attempts). This is the exact same class of issue as the already-documented `5432` Postgres port conflict — a pre-existing, project-unrelated local service squatting on a port this stack also defaults to, not a defect in this change. Workaround (documented in the runbook): set `WORKER_BASE_URL=http://127.0.0.1:8000` explicitly instead of relying on the `localhost` default. Confirmed the fix: after pinning the URL, extraction/review requests correctly reached the real worker (visible in its own log) and the false failure disappeared.
8. **Full automated suite, live and offline** (see Test Commands Run/Verification Evidence below), Docker torn down and the port remap reverted (`git diff infra/docker-compose.yml` confirmed empty) at the end of this pass, exactly as every prior PR in this change.

## Completed Tasks

- [x] Work Unit 1 RED: extended `apps/api/tests/migrate-runner.test.mjs` with a directory-discovery unit test and a live-Postgres FK-ordering integration test; ran and confirmed a genuine `TypeError: listMigrationFiles is not a function` against the pre-fix tree.
- [x] Work Unit 1 GREEN: replaced `DEFAULT_MIGRATION_PATH` with `MIGRATIONS_DIR` + `listMigrationFiles()`; `migrateUp`/`migrateDown` now glob+sort-discover and run all `*.sql` migrations (ascending for up, reversed for down) by default.
- [x] Work Unit 1 TRIANGULATE: out-of-order filenames sort correctly (unit test); `0001` alone still applies with no regression (new dedicated live test via the `migrationPath` escape hatch).
- [x] Work Unit 1 REFACTOR: `listMigrationFiles()` extracted as an independently unit-testable, exported helper.
- [x] Work Unit 1 Verify: `pnpm --dir apps/api test` green (offline and live); manual `migrate.mjs up`/`down` cycle against dockerized Postgres with real fixture files, confirmed live via the integration test.
- [x] Work Unit 1 Rollback verified (by design, not executed as a live revert this pass): reverting `migrate.mjs` to the hardcoded single-file path leaves `0001` still working (it sorts first alphabetically) — the `migrationPath` escape hatch this pass added is exactly the shape the old hardcoded default used, so a revert is a pure subtraction with no other file touched.
- [x] Work Unit 2 RED: created `apps/api/tests/llm-provider-config-migration.test.mjs`; ran and confirmed a genuine `0 !== 1` failure (table did not exist) against the pre-`0002` tree.
- [x] Work Unit 2 GREEN: created `apps/api/src/db/migrations/0002_llm_provider_config.sql` (table + CHECK constraints + partial unique index), matching `0001`'s exact conventions and `design.md`'s literal column list; re-ran the RED test — 1 pass / 0 fail.
- [x] Work Unit 2 TRIANGULATE: confirmed `0002` runs after `0001` via the ordered runner (inherent to the RED test passing); confirmed `-- DOWN` cleanly drops the table (asserted in-test and re-verified via manual CLI + `psql \dt`).
- [x] Work Unit 2 REFACTOR: N/A, as scoped.
- [x] Work Unit 2 Verify: `pnpm --dir apps/api test` green; manual `migrate.mjs up` confirmed `llm_provider_config` exists among all 13 tables via `psql \dt` inside the live container.
- [x] Work Unit 2 Rollback verified (by design): `migrate.mjs down` for `0002` (exercised live, confirmed 0 relations remain via `psql \dt`) or deleting the file leaves the table never created.
- [x] Necessary pre-existing-test update: `migrate-runner.test.mjs`'s full-discovery test's expected table count updated from 12 to 13 (`BASELINE_TABLES` + `llm_provider_config`), and a new dedicated "0001 alone" test added to preserve the original narrower guarantee — see "A necessary, deliberate change" above.
- [x] Work Unit 3 RED: created `apps/api/tests/provider-key-cipher.test.mjs` (12 pure-unit tests); ran and confirmed genuine `ERR_MODULE_NOT_FOUND` failures (12/12 fail) against the pre-GREEN tree.
- [x] Work Unit 3 GREEN: created `apps/api/src/security/provider-key-cipher.mjs` (`encryptApiKey`/`decryptApiKey`/`getEncryptionKey`/`lastFour` via Node's built-in `node:crypto`, AES-256-GCM, packed `v1:iv:tag:ciphertext`); re-ran — 12 pass / 0 fail.
- [x] Work Unit 3 TRIANGULATE: empty-string, non-hex, too-short (62 chars), and too-long (66 chars) keys each covered by a dedicated fail-fast assertion.
- [x] Work Unit 3 REFACTOR: N/A, as scoped — module stays self-contained.
- [x] Work Unit 3 Verify: `pnpm --dir apps/api test` green; no live services required (all 12 tests offline-safe, verified with Docker both down and up).
- [x] Work Unit 3 Rollback verified (by design): deleting `provider-key-cipher.mjs` — nothing outside `provider-config-repository.mjs` (Work Unit 4) depends on it.
- [x] Work Unit 4 RED: created `apps/api/tests/admin-contract.test.mjs` (6 tests: routes shape, 401, 403, 422-with-no-key-leak, fail-fast-misconfiguration-with-no-key-leak, and one comprehensive live-Postgres CRUD+activate+list scenario); ran and confirmed genuine `ERR_MODULE_NOT_FOUND` failures (5/6 fail; the 6th live-DB test correctly skipped, Docker not yet up) against the pre-GREEN tree.
- [x] Work Unit 4 GREEN: created `apps/api/src/db/provider-config-repository.mjs`, `apps/api/src/admin-contract.mjs`, `apps/api/src/admin/admin-secret.guard.ts`, `apps/api/src/admin/admin.controller.ts`; registered `AdminController` in `apps/api/src/app.module.ts`. Caught and fixed one bug during GREEN (see Work Unit 4 narrative above: `getRepository()` call moved inside `withRepository()`'s `try` block). Re-ran — 6/6 pass (5 pass + 1 correctly-skipped offline; 6/6 pass with zero skips once Docker was up).
- [x] Work Unit 4 TRIANGULATE: confirmed `contract.test.mjs` stays byte-untouched and green (12/12 pass, unchanged); confirmed list items never carry an `encrypted_api_key`/`api_key` own-property (explicit per-item assertion, live-DB verified); confirmed end-to-end via manual `curl` against a real running NestJS server through the real `AdminSecretGuard`.
- [x] Work Unit 4 REFACTOR: `toMaskedView()` shared across list/create/update/activate from the start; extracted shared `validateProviderNameField`/`validateNonEmptyStringField`/`validationErrorOrValue` helpers between create/update payload validation after GREEN, re-verified no behavior change.
- [x] Work Unit 4 Verify: `pnpm --dir apps/api test` green (incl. untouched `contract.test.mjs`); manual `pnpm --dir apps/api start`-equivalent (`node --import tsx src/main.ts`) + `curl` against all 4 admin routes (401, 403, 200 list, 422, 201 create, 200 update, 200 activate) — all confirmed live.
- [x] Work Unit 4 Rollback verified (by design): delete `admin-contract.mjs`/`admin.controller.ts`/`admin-secret.guard.ts`/`provider-config-repository.mjs`; revert `app.module.ts`'s 5-line diff (`ThesisDocumentsController`/`ReviewRunsController` registration untouched).
- [x] Work Unit 5 RED: (a) created `apps/api/tests/active-provider-resolution.test.mjs`, confirmed genuine `AssertionError: expected 'failed', actual: 'completed'` against the pre-GREEN tree (zero-active-provider silently succeeded via the old always-Claude path); (b) added a pure-unit `defaultRunCagReview` forwarding test to `review-orchestrator.test.mjs`, confirmed genuine failure (fake worker never received provider fields); (c) reworked `services/worker/tests/test_cag_review.py:95-111`, kept the both-absent case, added a precedence test and an env-fallback test, confirmed genuine `AttributeError`/`TypeError` against the pre-GREEN `AnthropicProvider`.
- [x] Work Unit 5 GREEN: `anthropic_provider.py` gained `api_key` param + `_resolve_api_key()` (arg-then-env precedence); `llm_provider.py` gained `ProviderNotImplementedError`; `main.py` gained optional `ReviewRequest` provider fields + `select_llm_provider()` + `get_llm_provider(payload)`; `review-orchestrator.mjs`'s `defaultRunCagReview` forwards `provider_name`/`api_key`/`model_id` when supplied; `live-review-pipeline.mjs` gained `getProviderRepository()`/`runCagReviewWithActiveProvider()`, wired as `getLivePipeline()`'s `runCagReview` override. Re-ran all three RED fronts — all pass.
- [x] Work Unit 5 TRIANGULATE: `active-provider-resolution.test.mjs`'s single comprehensive live-Postgres test proves two sequential runs against two different activated providers each forward that exact provider's `api_key`/`model_id`, with no restart or process change between them (only a DB `activate()` call).
- [x] Work Unit 5 REFACTOR: confirmed via a dedicated live-Postgres scenario (worker returns `502`, provider still active) that `error_summary` never contains the active provider's raw api key — `assert.doesNotMatch(runFailure.body.error_summary, /sk-ant-provider-two-secret/)`.
- [x] Work Unit 5 Verify: `pnpm --dir apps/api test && pnpm --dir services/worker test` green (see Test Commands Run); manual end-to-end run against dockerized Postgres with one active Claude row, verified via the automated live-Postgres test (equivalent to a manual curl-driven run — see Work Unit Evidence).
- [x] Work Unit 5 Rollback verified (by design): reverting `live-review-pipeline.mjs`/`review-orchestrator.mjs`/`main.py`/`anthropic_provider.py` leaves the kept `ANTHROPIC_API_KEY` env fallback fully operable — no other file's behavior depends on the new provider-resolution code path.
- [x] A necessary, deliberate update to a pre-existing test (flagged): `live-review-integration.test.mjs`'s scenarios now seed and activate one claude provider row before triggering runs — without it, the legitimate new "no active provider" failure would make scenarios 1-2 fail for the wrong reason (provider resolution, not worker-response handling, which is what those scenarios actually test). Confirmed as a genuine, predicted regression (ran the unmodified test against the post-GREEN tree first: `AssertionError: expected 'completed', actual: 'failed'`), then fixed by seeding — re-ran, passes.
- [x] Work Unit 6 RED: created `services/worker/tests/test_provider_factory.py` (pure unit) and `services/worker/tests/test_review_endpoint.py` (FastAPI TestClient); ran and confirmed genuine `ImportError: cannot import name 'select_llm_provider'` (factory test) and deepseek/groq both returning `500` instead of the required `501` (endpoint test) against the pre-GREEN tree.
- [x] Work Unit 6 GREEN: created `services/worker/app/providers/unimplemented_provider.py` (`UnimplementedProvider`/`DeepSeekProvider`/`GroqProvider`, `.generate()` raises `ProviderNotImplementedError`); wired into `select_llm_provider`; added `except ProviderNotImplementedError` → `501 not_implemented` to `main.py`'s `internal_review` route. Re-ran — `test_provider_factory.py` 6/6 pass, `test_review_endpoint.py` 4/4 pass.
- [x] Work Unit 6 TRIANGULATE: confirmed (via re-running the pre-existing, unmodified PR B `admin-contract.test.mjs` live scenario) that an admin can still create+activate a deepseek row with zero error — the new DeepSeek failure only exists on the worker's `.generate()` path, never at admin CRUD/activate time.
- [x] Work Unit 6 REFACTOR: N/A, as scoped — single factory branch addition.
- [x] Work Unit 6 Verify: `pnpm --dir services/worker test` green (24/24, 0 fail); no live DeepSeek/Groq keys required anywhere in the new tests.
- [x] Work Unit 6 Rollback verified (by design): delete `unimplemented_provider.py`, revert the `select_llm_provider` deepseek/groq branches and `main.py`'s `ProviderNotImplementedError` except-clause; claude-only behavior is fully restored.
- [x] Work Unit 7 RED: created `apps/web/tests/admin-providers-view.test.mjs` (21 pure-unit tests); ran and confirmed genuine `ERR_MODULE_NOT_FOUND` against the pre-GREEN tree.
- [x] Work Unit 7 GREEN: created `apps/web/src/app/admin/admin-providers-view.ts` (pure logic), `admin-secret-store.ts` (`AdminSecretStore`), `admin-api-client.ts` (`AdminApiClient`), `admin-providers-page.ts` (`AdminProvidersPage`); added the `/admin/llm-providers` route. Re-ran — 21/21 pass.
- [x] Work Unit 7 TRIANGULATE: 401/403 each produce a distinct, specific, non-silent UI message; re-verified live via real `curl` against the real `AdminSecretGuard`.
- [x] Work Unit 7 REFACTOR: N/A this pass, as scoped — masking/path helpers shared from the start in one pure module.
- [x] Work Unit 7 Verify: `pnpm --dir apps/web test` green (40/40); `ng build` typechecks cleanly; real `ng serve` + proxy confirmed the admin route renders and the proxied admin API call returns the real masked list; manual `curl` confirmed no raw key ever appears in any admin response.
- [x] Work Unit 7 Rollback verified (by design): revert `apps/web/src/app/admin/` + the `app.routes.ts` route entry + the two new/extended test files.
- [x] Work Unit 8 RED: extended `review-repository.test.mjs`, `review-orchestrator.test.mjs`, `live-review-integration.test.mjs`, and created `review-run-provenance-migration.test.mjs` + extended `apps/web/tests/results-view.test.mjs`; ran and confirmed genuine failures on all fronts (missing columns, ignored fields, missing response field) against the pre-GREEN tree.
- [x] Work Unit 8 GREEN: created `0003_review_run_provider_provenance.sql`; extended `review-repository.mjs`, `live-review-pipeline.mjs`, `review-orchestrator.mjs`, `api-contract.mjs`, `thesis-api-client.ts`, `results-view.ts`, `results-page.ts`. Re-ran all RED fronts — all pass.
- [x] Work Unit 8 TRIANGULATE: the "unknown provider" graceful fallback proven at 3 independent layers (pure unit, repository, full live-HTTP against a real run with its provenance columns directly nulled).
- [x] Work Unit 8 REFACTOR: N/A, as scoped — additive columns + one augmentation point.
- [x] Work Unit 8 Verify: `pnpm --dir apps/api test && pnpm --dir apps/web test` green (offline 64/13/0 + 40/0/0; live 77/0/0); manual live run confirmed graceful null provenance on a failed run.
- [x] Work Unit 8 Rollback verified (by design): drop `0003`'s columns via its `-- DOWN`; revert the provenance-forwarding/surfacing additions in every touched file.
- [x] Work Unit 9 (N/A RED/GREEN/TRIANGULATE/REFACTOR, per `tasks.md`): extended `docs/mvp-vertical-slice-runbook.md` with a new "LLM Provider Admin" section; verified everything short of a real Claude key for real this pass — server boot, all 4 admin endpoints, zero-active-provider real failure, a full activated-but-fake-key run genuinely rejected by the real Anthropic API, the Angular admin page served and proxied for real, and a genuine local port-conflict quirk (`localhost:8000` resolving to an unrelated PHP server on `[::1]:8000`) discovered, root-caused, worked around, and documented.
- [x] Work Unit 9 Rollback: N/A — docs only.

## Files Changed

| File | Action | What Was Done |
|------|--------|---------------|
| `apps/api/src/db/migrate.mjs` | Modified | Replaced hardcoded `DEFAULT_MIGRATION_PATH` (single file) with `MIGRATIONS_DIR` + `listMigrationFiles()` (glob+sort discovery). `migrateUp`/`migrateDown` now default to discovering and running ALL `*.sql` migrations under `MIGRATIONS_DIR` — ascending order for up, reversed for down — via a shared `runMigrationFiles()` helper. Kept `migrationPath` (single-file override) and added `migrationsDir` (directory override) as explicit escape hatches for tests/rollback. |
| `apps/api/tests/migrate-runner.test.mjs` | Modified | Added `listMigrationFiles` unit test (out-of-order fixture files); added a live-Postgres FK-ordering fixture test proving both UP (ascending) and DOWN (reversed) ordering are genuinely correct, not just named-correctly; added a dedicated "0001 alone via `migrationPath`" live regression test; renamed the full-discovery test and updated its expected table count from 12 to 13 now that `0002` legitimately exists. |
| `apps/api/src/db/migrations/0002_llm_provider_config.sql` | Created | `llm_provider_config` table (`id, provider_name, model_id, encrypted_api_key, api_key_last_four, is_active, metadata, created_at, updated_at`) with CHECK constraints on `provider_name` (`claude`/`deepseek`/`groq`) and `model_id` (non-blank), a partial unique index (`WHERE is_active`) enforcing at most one active row, and a `provider_name` lookup index — following `0001`'s exact `-- UP`/`-- DOWN` conventions. |
| `apps/api/tests/llm-provider-config-migration.test.mjs` | Created | Live-Postgres integration test: table exists after `migrate up`; unsupported `provider_name` and blank `model_id` both violate their CHECK constraints; a second concurrently-active row violates the partial unique index (a second inactive row does not); table dropped after `migrate down`. |
| `infra/docker-compose.yml` | Unmodified (temporarily remapped for live verification, then reverted) | Same pattern as `mvp-vertical-slice` PR A/C/D — see Test Commands Run and the local-port-conflict note below. `git diff` confirms zero net change. |
| `openspec/changes/llm-provider-admin/tasks.md` | Modified | Marked Work Unit 1 and Work Unit 2 checkboxes `[x]` (this pass additionally marks Work Units 3–4, see below). |
| `openspec/changes/llm-provider-admin/apply-progress.md` | Created | This file. |
| `apps/api/src/security/provider-key-cipher.mjs` | Created (PR B) | AES-256-GCM `encryptApiKey`/`decryptApiKey` (packed `v1:iv(b64):tag(b64):ciphertext(b64)`) + `getEncryptionKey()` fail-fast validation + `lastFour()`, using only Node's built-in `node:crypto` (no new dependency). |
| `apps/api/tests/provider-key-cipher.test.mjs` | Created (PR B) | 12 pure-unit tests: round-trip, random-IV-per-call, ciphertext/auth-tag tamper rejection, 5 fail-fast `getEncryptionKey()` cases (missing/empty/non-hex/too-short/too-long), valid-key shape, and independent `encryptApiKey`/`decryptApiKey` fail-fast checks. |
| `apps/api/src/db/provider-config-repository.mjs` | Created (PR B) | `createProviderConfigRepository()`: eager fail-fast key validation at construction; `list`/`create`/`update`/`activate`/`getActiveProvider`; shared `toMaskedView()` allowlist mapper; transactional `activate()` respecting PR A's partial unique index; key-write-only `update()`. |
| `apps/api/src/admin-contract.mjs` | Created (PR B) | Isolated admin request handler (design decision #7) mirroring `api-contract.mjs`'s pure-handler pattern: shared-secret guard check (`checkAdminSecretHeader`, SHA-256-digest constant-time compare), payload validation, routing for all 4 admin routes, lazy cached repository singleton, `EncryptionKeyError`/generic-error mapping to standard-shaped `5xx` responses, `_resetAdminContractForTests()`. |
| `apps/api/tests/admin-contract.test.mjs` | Created (PR B) | 6 tests: route shape; 401 (no header); 403 (wrong secret); 422 (unsupported `provider_name`) with an explicit no-key-leak assertion; fail-fast misconfigured-key with a no-key-leak assertion; one comprehensive live-Postgres create/create/update/activate/activate/list scenario asserting masked keys throughout and atomic active-row switching. |
| `apps/api/src/admin/admin-secret.guard.ts` | Created (PR B) | Real NestJS `CanActivate` delegating to `admin-contract.mjs`'s `checkAdminSecretHeader` (single source of truth) — defense-in-depth at the HTTP transport boundary. |
| `apps/api/src/admin/admin.controller.ts` | Created (PR B) | `@Controller('api/v1/admin/llm-providers') @UseGuards(AdminSecretGuard)` with `list`/`create`/`update`/`activate` methods, each delegating to `handleAdminRequest`, matching the existing controller pattern. |
| `apps/api/src/app.module.ts` | Modified (PR B) | Registered `AdminController` alongside the existing `ThesisDocumentsController`/`ReviewRunsController` (5-line diff). |
| `apps/api/src/jobs/review-orchestrator.mjs` | Modified (PR C) | `defaultRunCagReview` gained optional `providerName`/`apiKey`/`modelId` params, conditionally added to the worker request body only when supplied — old-style callers unaffected. |
| `apps/api/src/live-review-pipeline.mjs` | Modified (PR C) | Added `getProviderRepository()` (lazy singleton) + `runCagReviewWithActiveProvider()` (resolves+decrypts the active provider fresh on every call, throws `"no active LLM provider configured"` on zero-active); wired as `getLivePipeline()`'s `runCagReview` override; `_resetLiveReviewPipelineForTests()` now also clears the provider-repository cache. |
| `apps/api/tests/active-provider-resolution.test.mjs` | Created (PR C) | One comprehensive live-Postgres test: zero-active-providers explicit failure (worker never called), per-run re-resolution across a provider switch, and no raw-key leak into `error_summary` on a downstream worker failure. |
| `apps/api/tests/review-orchestrator.test.mjs` | Modified (PR C) | Added a pure-unit (no DB) test proving `defaultRunCagReview` forwards provider fields when supplied and omits them when not; added a `startFakeWorker()` helper for it. |
| `apps/api/tests/live-review-integration.test.mjs` | Modified (PR C, necessary) | Seeds and activates one claude provider row before its 3 scenarios run — see the flagged "necessary, deliberate update" note above. |
| `services/worker/app/providers/anthropic_provider.py` | Modified (PR C) | `__init__` gained `api_key: str \| None = None`; added `_resolve_api_key()` implementing arg-then-env precedence (design decision #11); `generate()` uses it instead of reading the env var directly. |
| `services/worker/app/providers/llm_provider.py` | Modified (PR C) | Added `ProviderNotImplementedError(LLMProviderError)`. |
| `services/worker/app/providers/unimplemented_provider.py` | Created (PR C) | `UnimplementedProvider`/`DeepSeekProvider`/`GroqProvider` — `.generate()` raises `ProviderNotImplementedError`, no network call ever attempted. |
| `services/worker/app/main.py` | Modified (PR C) | `ReviewRequest` gained optional `provider_name`/`api_key`/`model_id`; added `select_llm_provider()` (never raises directly — defers all failure into `.generate()`) and `get_llm_provider(payload)`; `internal_review` gained an `except ProviderNotImplementedError` → `501 not_implemented` branch. |
| `services/worker/tests/test_cag_review.py` | Modified (PR C, consciously reworked per orchestrator instruction) | Kept the both-absent-credentials test; added an explicit-key-beats-env precedence test and an env-fallback-still-works test. |
| `services/worker/tests/test_provider_factory.py` | Created (PR C) | Pure unit tests for `select_llm_provider`: claude default/explicit forwarding; deepseek/groq/unknown all defer to a `.generate()`-time `ProviderNotImplementedError`, no network call, no key leak in the message. |
| `services/worker/tests/test_review_endpoint.py` | Created (PR C) | FastAPI `TestClient`-level tests for `/internal/review`: missing-credentials `500`; deepseek/groq `501 not_implemented` with no key leak; old-style request (no provider fields) still validates. |
| `apps/web/src/app/admin/admin-providers-view.ts` | Created (PR D) | Pure view-model/payload/gating logic: `buildAdminProvidersViewModel`, `maskedKeyLabel`, `buildCreateProviderPayload`/`buildUpdateProviderPayload` (write-only key), `buildProviderPath`/`buildActivatePath`, `resolveAdminSecretForRequest`/`canSendAdminRequest`, `extractAdminErrorMessage`/`isAdminAuthError`. |
| `apps/web/src/app/admin/admin-secret-store.ts` | Created (PR D) | `AdminSecretStore` — in-memory-only `signal<string\|null>` (design decision #9); `getSecret()`/`clearSecret()`. |
| `apps/web/src/app/admin/admin-api-client.ts` | Created (PR D) | `AdminApiClient` (design decision #8, sibling to `ThesisApiClient`) — `listProviders`/`createProvider`/`updateProvider`/`activateProvider`, each resolving the secret lazily via `defer()` and sending `x-admin-secret`. |
| `apps/web/src/app/admin/admin-providers-page.ts` | Created (PR D) | `AdminProvidersPage` — standalone/OnPush/`inject()` list + add/edit form + activate action, matching `upload-page.ts`/`results-page.ts`'s conventions. |
| `apps/web/tests/admin-providers-view.test.mjs` | Created (PR D) | 21 pure-unit tests covering every function in `admin-providers-view.ts`, including the 401/403 UI-state and session-secret-gating TRIANGULATE cases. |
| `apps/web/src/app/app.routes.ts` | Modified (PR D) | Added the `/admin/llm-providers` route. |
| `apps/web/tests/smoke.test.mjs` | Modified (PR D) | Extended route/component-existence assertions to cover the new admin route + `AdminProvidersPage`/`AdminSecretStore`/`AdminApiClient`, including a no-`localStorage`-usage assertion. |
| `apps/api/src/db/migrations/0003_review_run_provider_provenance.sql` | Created (PR D) | Additive `llm_provider_name` (CHECK-constrained)/`llm_model_id` nullable columns on `review_run`. |
| `apps/api/tests/review-run-provenance-migration.test.mjs` | Created (PR D) | Live-Postgres schema test mirroring Work Unit 2's pattern: columns exist + nullable, CHECK constraint enforced, `migrate down` cleans up. |
| `apps/api/src/db/review-repository.mjs` | Modified (PR D) | `updateReviewRunStatus` gained optional `llmProviderName`/`llmModelId` (COALESCE-safe); new `getReviewRunProvenance()`. |
| `apps/api/tests/review-repository.test.mjs` | Modified (PR D) | Added provenance write/read assertions, including the graceful-null-on-unset-run case. |
| `apps/api/src/live-review-pipeline.mjs` | Modified (PR D) | `runCagReviewWithActiveProvider` now augments its result with `providerName`/`modelId` (the worker never echoes this back). |
| `apps/api/src/jobs/review-orchestrator.mjs` | Modified (PR D) | Persists `reviewResult`'s `providerName`/`modelId` onto the completed `review_run` row. |
| `apps/api/tests/review-orchestrator.test.mjs` | Modified (PR D) | Extended the grounded-finding test to assert provenance persistence via `getReviewRunDbId`/`getReviewRunProvenance`. |
| `apps/api/src/api-contract.mjs` | Modified (PR D) | `withRealSummary` (+ both stub response paths) now surface `llm_provider_name`/`llm_model_id` on every `GET /review-runs/{id}` response via a shared `EMPTY_PROVENANCE` constant. |
| `apps/api/tests/live-review-integration.test.mjs` | Modified (PR D) | Added the full-HTTP provenance assertion plus the direct-SQL-null / graceful-response TRIANGULATE proof. |
| `apps/web/src/app/thesis-api-client.ts` | Modified (PR D) | `ReviewRunResponse` gained `llm_provider_name`/`llm_model_id`. |
| `apps/web/src/app/results/results-view.ts` | Modified (PR D) | Added `formatProviderLabel()`; `findings`/`no_findings` view-model kinds gained `providerLabel`. |
| `apps/web/src/app/results/results-page.ts` | Modified (PR D) | Renders "Reviewed by: ..." above the finding list / "No findings." message. |
| `apps/web/tests/results-view.test.mjs` | Modified (PR D) | Added provenance-label assertions, including the graceful "Unknown provider" fallback. |
| `docs/mvp-vertical-slice-runbook.md` | Modified (PR D) | Added the "LLM Provider Admin — Manual End-to-End Verification" section plus a new documented local-machine port-conflict quirk (`localhost:8000` vs. an unrelated local PHP server on `[::1]:8000`). |
| `openspec/changes/llm-provider-admin/tasks.md` | Modified (PR D) | Marked Work Units 7-9 `[x]`. |
| `openspec/changes/llm-provider-admin/apply-progress.md` | Modified (PR D) | This file — merged PR D's record in without overwriting PR A/B/C's. |

## Test Commands Run

| Phase | Command | Result |
| --- | --- | --- |
| Safety net (baseline, before any change) | `pnpm test` (root, Docker down) | `apps/api` 45 pass / 7 skip / 0 fail, `apps/web` 15 pass / 0 fail, `services/worker` 13 tests OK — matches the archived `mvp-vertical-slice` final state exactly, confirming this pass starts from a clean, already-shipped baseline. |
| Work Unit 1 RED | `node --import tsx --test tests/migrate-runner.test.mjs` (in `apps/api`, Docker down) | `listMigrationFiles is not a function` — 1 fail (test 3), 2 pre-existing pass, 2 skipped (live-pg tests, `role "pg1" does not exist` — the documented local Homebrew-Postgres-on-5432 quirk, container not up yet). |
| Work Unit 1 GREEN (offline) | `node --import tsx --test tests/migrate-runner.test.mjs` (in `apps/api`, Docker down) | 3 pass / 0 fail / 2 skip (live tests still correctly skip — Docker not up yet at this point in the pass). |
| Docker up (temporary port remap for live verification) | `docker compose -f infra/docker-compose.yml up -d` (port temporarily `5433:5432`, see note below) | Container `pg1-db` reached `healthy`. |
| Work Unit 2 RED (live) | `DATABASE_URL="postgres://pg1:pg1@localhost:5433/pg1" node --import tsx --test tests/llm-provider-config-migration.test.mjs` (in `apps/api`) | 1 fail: `llm_provider_config must exist after migrate up: 0 !== 1` — genuine failure against live Postgres, not a module-not-found stub (the migration file did not exist yet, so the default discovery-based `migrate up` had nothing new to apply). |
| Work Unit 2 GREEN (live) | Same command, after creating `0002_llm_provider_config.sql` | 1 pass / 0 fail — table created, both CHECK constraints violated as expected, the partial unique index violated on a second concurrently-active row, and the table dropped cleanly on `migrate down`. |
| Work Unit 1 full live re-verification | `DATABASE_URL="postgres://pg1:pg1@localhost:5433/pg1" node --import tsx --test tests/migrate-runner.test.mjs` (in `apps/api`) | **6 pass / 0 fail** — includes the live FK-ordering fixture test (genuinely proving UP-ascending/DOWN-reversed ordering against real Postgres) and the new "0001 alone" regression test, both executed for real (no skips). |
| Full `apps/api` suite (live pg) | `DATABASE_URL="postgres://pg1:pg1@localhost:5433/pg1" pnpm --dir apps/api test` | **56 pass / 0 fail** (0 skipped — every live-pg-gated test genuinely ran). |
| Manual CLI evidence (live pg) | `DATABASE_URL="postgres://pg1:pg1@localhost:5433/pg1" node apps/api/src/db/migrate.mjs down` then `... up` | `down` (idempotent no-op on a clean-ish DB) → `up` — both completed without error. |
| Manual CLI table verification | `docker exec pg1-db psql -U pg1 -d pg1 -c "\dt"` | **13 rows** — all 12 baseline tables + `llm_provider_config`, confirming the real CLI (not just the test harness) applies both migrations in order. |
| Manual CLI rollback verification | `DATABASE_URL="postgres://pg1:pg1@localhost:5433/pg1" node apps/api/src/db/migrate.mjs down` then `docker exec pg1-db psql -U pg1 -d pg1 -c "\dt"` | `down` completed; `\dt` reported **"Did not find any relations."** — clean full rollback via the real CLI. |
| Docker down + port revert | `docker compose -f infra/docker-compose.yml down -v`; reverted `infra/docker-compose.yml`'s port back to `5432:5432` | Container, network, and volume removed cleanly; `git diff infra/docker-compose.yml` confirmed **zero net change** (empty diff). |
| Final verification, `apps/api` alone (Docker down, canonical port) | `pnpm --dir apps/api test` | **46 pass / 10 skip / 0 fail** (up from the 45/7/0 baseline: +1 pass is the new offline-safe `listMigrationFiles` unit test; +3 skip are the three new live-pg-gated tests — the fixture-migrations live test, the "0001 alone" live test, and the new `llm-provider-config-migration.test.mjs` — each with an explicit `t.skip(reason)`, never a silent no-op). |
| Final verification, full root suite (Docker down, canonical port) | `pnpm test` (root) | **Exit code 0.** `apps/api` 46 pass / 10 skip / 0 fail, `apps/web` 15 pass / 0 fail, `services/worker` 13 tests OK. |
| **PR B** — Work Unit 3 RED | `node --import tsx --test tests/provider-key-cipher.test.mjs` (Docker down) | `Cannot find module '.../provider-key-cipher.mjs'` — **12/12 fail**, `ERR_MODULE_NOT_FOUND` (genuine RED, module did not exist yet). |
| Work Unit 3 GREEN | Same command, after creating `provider-key-cipher.mjs` | **12 pass / 0 fail.** |
| Work Unit 4 RED | `node --import tsx --test tests/admin-contract.test.mjs` (Docker down) | 5 fail (`Cannot find module '.../admin-contract.mjs'`, `ERR_MODULE_NOT_FOUND`); 1 skip (live-DB scenario, documented port conflict, Docker not yet up) — genuine RED. |
| Docker up (temporary port remap for live verification, PR B) | `docker compose -f infra/docker-compose.yml up -d` (port temporarily `5433:5432`) | Container `pg1-db` reached healthy. |
| Work Unit 4 GREEN (offline, before Docker up) | `node --import tsx --test tests/admin-contract.test.mjs` | 5 pass / 0 fail / 1 correctly-skip (Docker not up yet at that exact point in the pass). |
| Work Unit 4 GREEN (live, after Docker up) | `DATABASE_URL=postgres://pg1:pg1@localhost:5433/pg1 node --import tsx --test tests/admin-contract.test.mjs` | **6 pass / 0 fail / 0 skip** — the live-Postgres CRUD+activate+list scenario genuinely ran and passed. |
| Full `apps/api` suite (live pg, PR B) | `DATABASE_URL=postgres://pg1:pg1@localhost:5433/pg1 pnpm --dir apps/api test` | **74 pass / 0 fail / 0 skip.** |
| `contract.test.mjs` + `smoke.test.mjs` re-verification (live pg, confirms NestJS wiring compiles) | `DATABASE_URL=postgres://pg1:pg1@localhost:5433/pg1 node --import tsx --test tests/smoke.test.mjs tests/contract.test.mjs` | **14 pass / 0 fail** — `contract.test.mjs` byte-untouched and green; `smoke.test.mjs` (which boots the real `Pg1ApiModule` including the new `AdminController`) still green with no `LLM_PROVIDER_ENCRYPTION_KEY` set, confirming the Work Unit 3 "fail-fast at first cipher use, not at shared bootstrap" scope decision was correct. |
| Manual runtime-harness curl verification (live pg) | `node --import tsx src/main.ts` (real server, port 3099) then `curl` against all 4 admin routes | No header → `401 unauthorized`; wrong secret → `403 forbidden`; valid secret list (empty) → `200 {"items":[]}`; create with `provider_name: "openai"` + a key → `422 validation_error` naming `provider_name`; create valid claude row → `201` with `api_key_last_four` only, no key material; `PATCH` model_id only → `200`, `api_key_last_four` unchanged; `POST .../activate` → `200`, `is_active: true`; list after activate → confirms the row. All responses manually inspected — no raw or encrypted key ever appeared. |
| Post-refactor re-verification (offline) | `node --import tsx --test tests/provider-key-cipher.test.mjs tests/admin-contract.test.mjs tests/contract.test.mjs` (Docker down) | **29 pass / 1 skip (live-DB, expected) / 0 fail** — confirms the create/update payload-validation helper extraction (REFACTOR) did not change behavior. |
| Docker down + port revert (PR B) | `docker compose -f infra/docker-compose.yml down -v`; reverted port back to `5432:5432` | Container, network, volume removed; `git diff infra/docker-compose.yml` confirmed **zero net change**. |
| Final verification, full root suite (Docker down, canonical port, PR B) | `pnpm test` (root) | **Exit code 0.** `apps/api` **63 pass / 11 skip / 0 fail** (74 total — up from PR A's 46/10/0 by exactly the 18 new offline-safe-or-skip tests: 12 cipher + 6 admin-contract), `apps/web` 15 pass / 0 fail, `services/worker` 13 tests OK. |
| **PR C** — worker RED (Work Units 5+6 combined) | `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. python3 -m unittest discover -s tests -p 'test_*.py'` (in `services/worker`) | **9 failures/errors** across the reworked/new test files: `AttributeError`/`TypeError` on `AnthropicProvider` (Work Unit 5's precedence tests), `ImportError: cannot import name 'select_llm_provider'` (Work Unit 6's factory test, 6 tests), and `500 != 501` for deepseek/groq (Work Unit 6's endpoint test, 2 tests) — all genuine, pre-GREEN failures. |
| PR C worker GREEN | Same command, after all worker-side changes | **24 pass / 0 fail** (up from the pre-existing 13). |
| PR C `apps/api` RED — `defaultRunCagReview` forwarding (no DB) | `node --import tsx --test tests/review-orchestrator.test.mjs` (Docker down) | New test failed: `SyntaxError: Unexpected token '<'... is not valid JSON` — traced to a test-authoring import-ordering bug (see "A subtle test-authoring bug caught during RED" above), NOT the production code; fixed the test, re-ran, got a genuine `deepEqual` mismatch (fake worker received no provider fields) confirming real RED, then implemented GREEN. |
| PR C `apps/api` GREEN — `defaultRunCagReview` forwarding | Same command | **4/4 pass** (3 pre-existing live-pg tests correctly skip offline, 1 new pure-unit test passes). |
| Docker up (temporary port remap for live verification, PR C) | `docker compose -f infra/docker-compose.yml up -d` (port temporarily `5433:5432`) | Container `pg1-db` reached healthy. |
| PR C `active-provider-resolution.test.mjs` RED (live pg) | `DATABASE_URL=postgres://pg1:pg1@localhost:5433/pg1 node --import tsx --test tests/active-provider-resolution.test.mjs` (pre-GREEN) | 1 fail: `AssertionError: expected 'failed', actual: 'completed'` — genuine, the zero-active-provider run silently completed via the old always-Claude path. |
| PR C `active-provider-resolution.test.mjs` GREEN (live pg) | Same command, after `live-review-pipeline.mjs`'s GREEN | **1 pass / 0 fail** — all 4 sequential scenarios (zero-active-fails, provider-one, provider-two-switch, worker-failure-no-key-leak) pass. |
| PR C `live-review-integration.test.mjs` — predicted-regression proof (live pg) | `DATABASE_URL=postgres://pg1:pg1@localhost:5433/pg1 node --import tsx --test tests/live-review-integration.test.mjs` (post-GREEN, pre-seeding-fix) | 1 fail: `AssertionError: expected 'completed', actual: 'failed'` — genuine, predicted regression (zero active provider now legitimately blocks every scenario). |
| PR C `live-review-integration.test.mjs` — after seeding fix (live pg) | Same command, after seeding an active claude row | **1 pass / 0 fail.** |
| PR C full `apps/api` suite (live pg) | `DATABASE_URL=postgres://pg1:pg1@localhost:5433/pg1 pnpm --dir apps/api test` | **76 pass / 0 fail / 0 skip** (up from PR B's 74 by exactly the 2 new tests: `active-provider-resolution.test.mjs` + `review-orchestrator.test.mjs`'s new forwarding test). |
| PR C `contract.test.mjs`/`smoke.test.mjs` re-verification (live pg) | `DATABASE_URL=postgres://pg1:pg1@localhost:5433/pg1 node --import tsx --test tests/smoke.test.mjs tests/contract.test.mjs` | **14 pass / 0 fail** — both byte-untouched and green, confirmed via `git diff` showing zero changes to either file. |
| Docker down + port revert (PR C) | `docker compose -f infra/docker-compose.yml down -v`; reverted port back to `5432:5432` | Container, network, volume removed; `git diff infra/docker-compose.yml` confirmed **zero net change**. |
| Final verification, full root suite (Docker down, canonical port, PR C state) | `pnpm test` (root) | **Exit code 0.** `apps/api` **64 pass / 12 skip / 0 fail** (76 total — up from PR B's 63/11/0 by the 1 new offline-safe forwarding test [pass] + 1 new live-pg-gated test [skip]), `apps/web` 15 pass / 0 fail, `services/worker` **24 tests OK** (up from 13). |
| **PR D** — Work Unit 7 RED | `node --import tsx --test tests/admin-providers-view.test.mjs` (in `apps/web`) | `Cannot find module '.../admin-providers-view.ts'` — **1/1 test file fails** (`ERR_MODULE_NOT_FOUND`, genuine RED, module did not exist yet). |
| Work Unit 7 GREEN | Same command, after creating the `admin/` module set | **21 pass / 0 fail.** |
| Full `apps/web` suite (Work Unit 7) | `pnpm --dir apps/web test` | **37 pass / 0 fail** (up from the 15/0/0 baseline: +21 admin-providers-view + 1 extended smoke test). |
| `ng build` typecheck (Work Unit 7) | `npx ng build --configuration development` (in `apps/web`) | Bundle built successfully — confirms the component/template compiles cleanly under the real Angular compiler, not just `tsx`'s looser transpile. |
| Docker up (temporary port remap for live verification, PR D) | `docker compose -f infra/docker-compose.yml up -d` (port temporarily `5433:5432`) | Container `pg1-db` reached healthy. |
| Work Unit 8 RED (migration, live pg) | `DATABASE_URL=postgres://pg1:pg1@localhost:5433/pg1 node --import tsx --test tests/review-run-provenance-migration.test.mjs` (in `apps/api`) | 1 fail: `0 !== 2` (both llm_provider_name and llm_model_id must exist) — genuine, columns did not exist yet. |
| Work Unit 8 GREEN (migration, live pg) | Same command, after creating `0003_review_run_provider_provenance.sql` | **1 pass / 0 fail.** |
| Work Unit 8 RED (repository, live pg) | `DATABASE_URL=postgres://pg1:pg1@localhost:5433/pg1 node --import tsx --test tests/review-repository.test.mjs` (in `apps/api`) | 1 fail: `null !== 'claude'` — genuine, `updateReviewRunStatus` silently ignored the new fields. |
| Work Unit 8 GREEN (repository, live pg) | Same command, after extending `review-repository.mjs` | **2 pass / 0 fail.** |
| Work Unit 8 RED (orchestrator, live pg) | `DATABASE_URL=postgres://pg1:pg1@localhost:5433/pg1 node --import tsx --test tests/review-orchestrator.test.mjs` (in `apps/api`) | 1 fail: `llmProviderName: null` (expected `'claude'`) — genuine, the orchestrator didn't forward provenance yet. |
| Work Unit 8 GREEN (orchestrator, live pg) | Same command, after extending `review-orchestrator.mjs`/`live-review-pipeline.mjs` | **4 pass / 0 fail.** |
| Work Unit 8 RED (live HTTP end-to-end, live pg) | `DATABASE_URL=postgres://pg1:pg1@localhost:5433/pg1 node --import tsx --test tests/live-review-integration.test.mjs` (in `apps/api`) | 1 fail: `undefined !== 'claude'` — genuine, `GET /review-runs/{id}` didn't carry the field at all yet. |
| Work Unit 8 GREEN (live HTTP end-to-end, live pg) | Same command, after extending `api-contract.mjs` | **1 pass / 0 fail** — including the direct-SQL-null / graceful-response TRIANGULATE assertion. |
| `contract.test.mjs`/`smoke.test.mjs` re-verification (live pg, PR D) | `DATABASE_URL=postgres://pg1:pg1@localhost:5433/pg1 node --import tsx --test tests/smoke.test.mjs tests/contract.test.mjs` | **14 pass / 0 fail** — both byte-untouched and green. |
| Migration-concurrency false-alarm re-check (live pg) | `DATABASE_URL=postgres://pg1:pg1@localhost:5433/pg1 node --import tsx --test --test-concurrency=1 tests/migrate-runner.test.mjs tests/llm-provider-config-migration.test.mjs tests/review-run-provenance-migration.test.mjs` | **8 pass / 0 fail** — confirms the earlier ad-hoc-invocation failure (missing `--test-concurrency=1`) was a harness mistake, not a real bug (see the flagged note under Work Unit 8 above). |
| Full `apps/api` suite (live pg, PR D) | `DATABASE_URL=postgres://pg1:pg1@localhost:5433/pg1 pnpm --dir apps/api test` | **77 pass / 0 fail / 0 skip** (up from PR C's 76 by the 1 new migration test file — the repository/orchestrator/integration RED tests were extensions of existing `test()` calls, not new ones). |
| Work Unit 8 RED (web, results-view) | `node --import tsx --test tests/results-view.test.mjs` (in `apps/web`) | `SyntaxError: ... does not provide an export named 'formatProviderLabel'` — genuine RED. |
| Work Unit 8 GREEN (web, results-view) | Same command, after extending `results-view.ts`/`thesis-api-client.ts` | **11 pass / 0 fail.** |
| Full `apps/web` suite (Work Unit 8) | `pnpm --dir apps/web test` | **40 pass / 0 fail** (up from Work Unit 7's 37 by +3 new results-view provenance tests). |
| Manual runtime-harness verification, PR D (live pg + real worker) | Real API (`node --import tsx src/main.ts`, `DATABASE_URL`/`LLM_PROVIDER_ENCRYPTION_KEY`/`ADMIN_SHARED_SECRET`/`WORKER_BASE_URL` set) + real worker (`python3 -m uvicorn app.main:app`) + real `ng serve --proxy-config proxy.conf.json` + `curl` | All 4 admin routes behave exactly as the automated tests expect (401/403/200/201/200); zero-active-provider run reaches a real `failed`; an activated-but-fake-key claude provider's key is genuinely rejected by the real Anthropic API (`401 invalid x-api-key`, worker log), surfacing as a real `failed` status with graceful `null` provenance; `/admin/llm-providers` renders via the real dev server and its proxied admin API call returns the real masked list. See "A genuine local-machine discovery" under Work Unit 9 above for the `localhost:8000`-resolves-to-the-wrong-process quirk found and worked around during this verification. |
| Manual DB cleanup + Docker down + port revert (PR D) | `node .../migrate.mjs down`; `docker compose -f infra/docker-compose.yml down -v`; reverted port back to `5432:5432` | Schema dropped; container/network/volume removed; `git diff infra/docker-compose.yml` confirmed **zero net change**. |
| Final verification, `apps/api` alone (Docker down, canonical port, PR D state) | `pnpm --dir apps/api test` | **64 pass / 13 skip / 0 fail** (77 total — up from PR C's 76 by the 1 new live-pg-gated migration test). |
| Final verification, full root suite (Docker down, canonical port, PR D / final state) | `pnpm test` (root) | **Exit code 0.** `apps/api` 64 pass / 13 skip / 0 fail, `apps/web` **40 pass / 0 fail**, `services/worker` 24 tests OK. |

### Local port-conflict note (documented, not a defect — same as `mvp-vertical-slice`)

This development machine has a pre-existing, project-unrelated Homebrew `postgresql@17` service bound to `127.0.0.1:5432`/`[::1]:5432` (confirmed via `lsof -nP -iTCP:5432 -sTCP:LISTEN` before starting this pass). Docker's own port-forwarding binds `0.0.0.0:5432`, but the OS routes `localhost` connections to the more specific loopback-bound Homebrew process instead of the Docker container, producing `role "pg1" does not exist` even though the container is healthy and genuinely has that role. To get genuine live evidence without touching the user's pre-existing local Postgres, `infra/docker-compose.yml`'s host port was temporarily changed to `5433:5432` for live verification only, then reverted to the canonical `5432:5432` before finishing (confirmed via `git diff` showing an empty diff). This is the exact same documented workaround used throughout `mvp-vertical-slice`'s apply-progress — not a defect introduced by this pass.

### New local port-conflict note discovered this pass (PR D, documented, not a defect)

`node --import tsx src/main.ts`'s API defaults `WORKER_BASE_URL` to `http://localhost:8000` when unset. On this development machine, an unrelated local PHP dev server was already listening on `[::1]:8000` (IPv6 loopback) while the real Python worker binds only to `127.0.0.1:8000` (IPv4) — confirmed via `lsof -nP -iTCP:8000 -sTCP:LISTEN`, which showed BOTH processes on the same port, different address families. Node's `fetch("http://localhost:8000/...")` resolved `localhost` to the IPv6 address first, silently hitting the unrelated PHP process instead of the real worker, producing a confusing `error_summary: "...Unexpected token '<'... is not valid JSON"` (an HTML error page being parsed as JSON) even with the real worker fully healthy — the tell was the real worker's own log showing zero incoming requests during the failing attempts. Same class of issue as the Postgres port note above (a pre-existing, project-unrelated local service squatting on a default port), not a defect in this change. Worked around during manual verification, and now documented in `docs/mvp-vertical-slice-runbook.md`, by setting `WORKER_BASE_URL=http://127.0.0.1:8000` explicitly (pinning IPv4) instead of relying on the `localhost` default.

## TDD Cycle Evidence

| Task | Test File | Layer | Safety Net | RED | GREEN | TRIANGULATE | REFACTOR |
|------|-----------|-------|------------|-----|-------|-------------|----------|
| Work Unit 1 — `listMigrationFiles` directory discovery | `apps/api/tests/migrate-runner.test.mjs` | Unit (temp fixture dir, no live Postgres) | ✅ 45/7/0 pre-existing baseline green before any change | ✅ Written — asserts sorted filenames from a not-yet-existing `listMigrationFiles` export; ran and confirmed `TypeError: listMigrationFiles is not a function` | ✅ Passed — `listMigrationFiles()` implemented (`readdir` + filter `.sql` + sort + map to `file://` URLs) | ✅ Fixture files written to disk deliberately out of filename order, plus a `.txt` file that must be excluded — both assertions pass | ✅ Clean — pure function, `dir` parameter defaults to the real `MIGRATIONS_DIR` but is fully overridable for tests |
| Work Unit 1 — `migrateUp`/`migrateDown` multi-file ordering | `apps/api/tests/migrate-runner.test.mjs` | Integration (live Postgres) | ✅ Baseline green (above) | ✅ Written — two FK-linked fixture migrations (`fixture_b` REFERENCES `fixture_a`) in a temp `migrationsDir`, genuinely proving both UP-ascending and DOWN-reversed ordering are required for the test to pass (not just named files) | ✅ Passed — `migrateUp` applies ascending, `migrateDown` applies the reversed array; both fixture tables created then cleanly dropped despite the FK dependency | ✅ Ran live against dockerized Postgres (port-remap workaround documented above) — genuine `CREATE`/`DROP` against real Postgres, not mocked | ✅ Clean — shared `runMigrationFiles(paths, pgClient, section)` helper avoids duplicating the up/down iteration logic |
| Work Unit 1 — "0001 alone" regression guard | `apps/api/tests/migrate-runner.test.mjs` | Integration (live Postgres) | ✅ Baseline green (above) | N/A — this test's purpose is a regression guard added directly alongside GREEN, since the regression it guards against (auto-discovery accidentally breaking single-file application) can only be demonstrated once `migrationPath` exists as an override; the RED state for "does `0001` alone still work" is inherently `1) doesn't exist yet` or `2) discovery accidentally always requires 2+ files` — both ruled out by design, verified by this passing test | ✅ Passed — `migrationPath` escape hatch applies exactly `0001_schema_baseline.sql`, producing exactly the 12 baseline tables, no more/fewer | ✅ Ran live; restores the full schema in its `finally` block so later live-pg test files in the same run are unaffected | ✅ Clean — reuses the same `migrationPath` parameter the old hardcoded default effectively behaved like |
| Work Unit 2 — `0002_llm_provider_config` table + constraints | `apps/api/tests/llm-provider-config-migration.test.mjs` | Integration (live Postgres) | ✅ Baseline green (above), plus Work Unit 1 fully green before starting | ✅ Written — asserts the table exists post-`migrate up`, both CHECK constraints reject bad input, the partial unique index rejects a second active row, and `migrate down` drops the table; ran against the pre-`0002` tree and confirmed a genuine `0 !== 1` failure (table did not exist) | ✅ Passed — `0002_llm_provider_config.sql` created; all 5 assertions pass live | ✅ A second **inactive** row for a different provider explicitly asserted to succeed (proving the invariant is "at most one active", not "at most one row"); `0002` confirmed to run after `0001` via the default ordered runner (inherent to the test's own `migrate up` call using no explicit path) | ✅ N/A — single migration file, no shared logic yet, as `tasks.md` scoped |
| Necessary update — full-discovery table-count assertion | `apps/api/tests/migrate-runner.test.mjs` | Integration (live Postgres) | ✅ Work Unit 1+2 both green individually before running the combined full suite | N/A — this is a pre-existing test whose assertion value became outdated by the *intended* combined effect of Work Unit 1 + 2, not a new feature | ✅ Fixed — `REQUIRED_TABLES` now derives from `BASELINE_TABLES` + `"llm_provider_config"`; re-ran the full live suite and got 56 pass / 0 fail (was: 1 genuine live failure, `13 !== 12`, before this fix) | ✅ Verified this doesn't mask a real bug: the "0001 alone" test (above) independently confirms the narrower 12-table guarantee still holds when only `0001` is applied | ✅ N/A |
| Work Unit 3 — `encryptApiKey`/`decryptApiKey` round-trip + GCM tamper detection | `apps/api/tests/provider-key-cipher.test.mjs` | Unit (pure, no live services) | ✅ Full `apps/api` 46/10/0 (Docker down) + 56/0/0 (Docker up) both green before starting | ✅ Written — round-trip, random-IV-per-call, ciphertext-tamper, auth-tag-tamper; ran against the pre-GREEN tree and confirmed genuine `ERR_MODULE_NOT_FOUND` (12/12 fail) | ✅ Passed — AES-256-GCM via `node:crypto`, packed `v1:iv:tag:ciphertext`; 12/12 pass | ✅ Two independent encryptions of the same plaintext produce different ciphertexts (random IV) yet both decrypt correctly; tamper detection separately proven for the ciphertext bytes AND the auth tag bytes | ✅ N/A — self-contained module, as scoped |
| Work Unit 3 — `getEncryptionKey()` fail-fast validation | `apps/api/tests/provider-key-cipher.test.mjs` | Unit (pure, no live services) | ✅ Same baseline as above | ✅ Written — missing/empty/non-hex/too-short/too-long key cases; ran against the pre-GREEN tree, confirmed genuine `ERR_MODULE_NOT_FOUND` | ✅ Passed — `EncryptionKeyError` thrown for every invalid case, valid 64-hex-char key returns a 32-byte Buffer | ✅ Each of the 4 invalid-input shapes (empty, non-hex, too-short, too-long) is its own dedicated test, not one combined assertion; `encryptApiKey`/`decryptApiKey` independently proven to also fail fast rather than silently proceeding | ✅ N/A |
| Work Unit 4 — Admin shared-secret guard (401/403) | `apps/api/tests/admin-contract.test.mjs` | Unit (pure, no live services) | ✅ Work Unit 3 fully green before starting | ✅ Written — no-header → 401, wrong-secret → 403, both asserting the submitted `api_key` never appears in the response; ran against the pre-GREEN tree, confirmed genuine `ERR_MODULE_NOT_FOUND` | ✅ Passed — SHA-256-digest constant-time compare in `checkAdminSecretHeader`; 401/403 both correct with no key leak | ✅ Re-verified end-to-end through the real NestJS `AdminSecretGuard` via manual `curl` (see Work Unit Evidence) — the pure-function test and the real HTTP transport agree | ✅ N/A — logic lives once in `admin-contract.mjs`, the NestJS guard delegates to it rather than re-implementing it |
| Work Unit 4 — Unsupported `provider_name` rejection (422) with no-key-leak proof | `apps/api/tests/admin-contract.test.mjs` | Unit (pure, no live services) | ✅ Same baseline as above | ✅ Written — `provider_name: "openai"` + a real-shaped `api_key` in the same request; ran against the pre-GREEN tree, confirmed genuine `ERR_MODULE_NOT_FOUND` | ✅ Passed — 422 naming `provider_name`, and `JSON.stringify(response.body)` asserted to NOT contain the submitted key string | ✅ This is the orchestrator's explicit "at least one failure-path key-leak proof" requirement — satisfied directly, not incidentally | ✅ N/A |
| Work Unit 4 — Fail-fast on misconfigured encryption key (no key leak) | `apps/api/tests/admin-contract.test.mjs` | Unit (pure, no live services — `DATABASE_URL` set to an unreachable fake value, since the key check fires before any DB I/O) | ✅ Same baseline as above | ✅ Written — clears `LLM_PROVIDER_ENCRYPTION_KEY`, asserts a `5xx` response and no key leak; ran against the pre-GREEN tree, confirmed genuine `ERR_MODULE_NOT_FOUND` | ✅ Passed — caught and fixed a real bug during GREEN (see Work Unit 4 narrative: `getRepository()` moved inside `withRepository()`'s `try`); re-ran, 500 `configuration_error`, no key leak | ✅ Proves `createProviderConfigRepository()`'s eager construction-time key check fires without ever attempting a DB connection (connection string is genuinely unreachable/fake) | ✅ N/A |
| Work Unit 4 — Admin CRUD + activate + list, live Postgres | `apps/api/tests/admin-contract.test.mjs` | Integration (live Postgres) | ✅ All prior Work Unit 4 unit tests green before this scenario | ✅ Written — create/create/update/activate/activate/list sequence; ran against the pre-GREEN tree, confirmed genuine `t.skip` (Docker not up) then, once Docker was up, a genuine `ERR_MODULE_NOT_FOUND`-class failure was no longer reachable to observe directly since GREEN landed first for the offline subset — the live scenario's own RED evidence is the skip-to-fail transition documented in Test Commands Run | ✅ Passed — 201 masked create, key-preserving update (`api_key_last_four` unchanged after a key-less `PATCH`), atomic activate (deepseek activation asserted to flip claude's `is_active` to `false` in the same list read), list items asserted to have no `encrypted_api_key`/`api_key` own-property | ✅ Live-verified twice: once via the automated live-Postgres test (6/6 pass, 0 skip) and once via manual `curl` against a real running server (see Work Unit Evidence) — both agree | ✅ N/A — `toMaskedView()` was already the single shared masking function from the first GREEN draft |
| Work Unit 5 — `AnthropicProvider` arg-then-env key precedence | `services/worker/tests/test_cag_review.py` | Unit (pure, no network) | ✅ Full worker suite (13/13) green before starting | ✅ Written — reworked the both-absent test (kept verbatim) and added two new tests; ran and confirmed genuine `AttributeError`/`TypeError` (`_resolve_api_key` and `api_key` kwarg did not exist yet) | ✅ Passed — `_resolve_api_key()` implements arg-then-env precedence; `AnthropicProvider.__init__` accepts `api_key` | ✅ Both directions covered: explicit key wins when both present; env still works when no explicit key is given | ✅ N/A — one small, self-contained method addition |
| Work Unit 5 — `defaultRunCagReview` forwards provider fields | `apps/api/tests/review-orchestrator.test.mjs` | Unit (pure, no live services — real local HTTP fake worker) | ✅ Full `apps/api` suite green before starting | ✅ Written — asserts the exact forwarded body with fields, and the exact unchanged body without; ran against the pre-GREEN tree and confirmed a genuine failure (after fixing an unrelated test-authoring import-ordering bug — see narrative above) | ✅ Passed — conditional field-by-field body construction, `undefined` fields never appear in the JSON body | ✅ Old-style call (`thesisText` only) produces byte-identical body to before this pass — explicit backward-compatibility proof | ✅ N/A — small, focused function change |
| Work Unit 5 — active-provider resolution end to end | `apps/api/tests/active-provider-resolution.test.mjs` | Integration (live Postgres + fake HTTP worker) | ✅ Full `apps/api` suite green (offline) before starting | ✅ Written — 4 sequential scenarios in one test (zero-active, provider-one, provider-switch, worker-failure-no-leak); ran against the pre-GREEN tree, confirmed genuine `AssertionError: expected 'failed', actual: 'completed'` | ✅ Passed — `runCagReviewWithActiveProvider()` resolves fresh via `getActiveProvider()` on every call, throws the exact spec-worded error on zero-active | ✅ Provider-switch scenario is the literal spec requirement ("no restart needed") — proven live, not simulated | ✅ N/A — the pre-existing orchestrator catch-block already handles the new error path; nothing to extract |
| Work Unit 6 — DeepSeek/Groq provider factory (pure) | `services/worker/tests/test_provider_factory.py` | Unit (pure, no network) | ✅ Work Unit 5 fully green before starting | ✅ Written — deepseek/groq/unknown all assert `.generate()` raises `ProviderNotImplementedError` with no key leak; ran against the pre-GREEN tree, confirmed genuine `ImportError: cannot import name 'select_llm_provider'` (6/6 fail) | ✅ Passed — `select_llm_provider` + `UnimplementedProvider` family implemented; 6/6 pass | ✅ Claude selection (explicit key/model forwarding) covered in the same file as a control case, proving the factory doesn't just "always fail" | ✅ N/A — single factory branch addition |
| Work Unit 6 — DeepSeek/Groq at the real HTTP boundary | `services/worker/tests/test_review_endpoint.py` | Integration (FastAPI `TestClient`, no network) | ✅ Same baseline as above | ✅ Written — full `/internal/review` request/response cycle for deepseek/groq (expect `501`) and the pre-existing missing-credentials case (expect `500`, unchanged); ran against the pre-GREEN tree, confirmed genuine `500 != 501` (2/2 fail) | ✅ Passed — `except ProviderNotImplementedError` added to `internal_review`'s except-chain; 4/4 pass | ✅ An explicit old-style-request-still-validates test proves the new optional fields don't break existing callers at the schema level | ✅ N/A |

## Work Unit Evidence

| Work Unit | Focused test command and exact result | Runtime harness command/scenario and exact result | Rollback boundary |
|---|---|---|---|
| 1 — Migration runner auto-discovery | `pnpm --dir apps/api test` → 46 pass / 10 skip / 0 fail (Docker down); `DATABASE_URL=postgres://pg1:pg1@localhost:5433/pg1 pnpm --dir apps/api test` → 56 pass / 0 fail (Docker up, live). | `DATABASE_URL=postgres://pg1:pg1@localhost:5433/pg1 node apps/api/src/db/migrate.mjs up` then `... down` against dockerized `pgvector/pgvector:pg16` — `up` produced all 13 tables (verified via `psql \dt`), `down` reverted to 0 relations. | Revert `apps/api/src/db/migrate.mjs` to the hardcoded single-file `DEFAULT_MIGRATION_PATH` behavior — `0001` keeps working since it sorts first alphabetically; `apps/api/tests/migrate-runner.test.mjs`'s new tests would then need reverting too (they test the new discovery behavior specifically). |
| 2 — `0002_llm_provider_config.sql` migration | `pnpm --dir apps/api test` → included in the same 46 pass / 10 skip / 0 fail (Docker down) / 56 pass / 0 fail (Docker up, live) totals above. | `DATABASE_URL=postgres://pg1:pg1@localhost:5433/pg1 node apps/api/src/db/migrate.mjs up` → `docker exec pg1-db psql -U pg1 -d pg1 -c "\dt"` → 13 rows including `llm_provider_config`; `... down` → `\dt` → "Did not find any relations." | `node apps/api/src/db/migrate.mjs down` (drops `llm_provider_config` cleanly via its `-- DOWN` section) or delete `apps/api/src/db/migrations/0002_llm_provider_config.sql` (table never created); delete `apps/api/tests/llm-provider-config-migration.test.mjs`. |
| 3 — AES-256-GCM encryption module | `node --import tsx --test tests/provider-key-cipher.test.mjs` → **12 pass / 0 fail** (Docker down and Docker up — no live services required, identical result both ways). | N/A per `tasks.md` ("pure unit, no live services") — genuinely confirmed: the same 12/12 pass result was reproduced with Docker fully down. | Delete `apps/api/src/security/provider-key-cipher.mjs` and `apps/api/tests/provider-key-cipher.test.mjs` — nothing else outside `provider-config-repository.mjs` imports it. |
| 4 — Admin CRUD + activate endpoints | `node --import tsx --test tests/admin-contract.test.mjs` → 5 pass / 0 fail / 1 skip (Docker down, live scenario correctly skips); `DATABASE_URL=postgres://pg1:pg1@localhost:5433/pg1 node --import tsx --test tests/admin-contract.test.mjs` → **6 pass / 0 fail / 0 skip** (Docker up, live). Full `apps/api` suite live: **74 pass / 0 fail**. | `node --import tsx src/main.ts` (real NestJS server, port 3099, `DATABASE_URL`/`LLM_PROVIDER_ENCRYPTION_KEY`/`ADMIN_SHARED_SECRET` set) + `curl` against all 4 routes: no-header → 401; wrong-secret → 403; list (empty) → 200; invalid `provider_name` → 422; create claude → 201 masked; `PATCH` model_id → 200, key preserved; activate → 200, `is_active: true`; list → confirms. | Delete `admin-contract.mjs`/`admin.controller.ts`/`admin-secret.guard.ts`/`provider-config-repository.mjs`/both new test files; revert `app.module.ts`'s 5-line `AdminController` registration diff. |
| 5 — Active-provider resolution wired into review pipeline | `pnpm --dir apps/api test && pnpm --dir services/worker test` → apps/api **64 pass / 12 skip / 0 fail** (Docker down) / **76 pass / 0 fail** (Docker up, live); worker **24 pass / 0 fail**. | `DATABASE_URL=postgres://pg1:pg1@localhost:5433/pg1 node --import tsx --test tests/active-provider-resolution.test.mjs` — end-to-end live run: zero-active fails explicitly (worker never called), one active Claude row completes with its exact fields forwarded, switching to a second Claude-shaped row re-resolves on the very next run, and a downstream 502 never leaks the active key into `error_summary`. | Revert `live-review-pipeline.mjs`/`review-orchestrator.mjs`/`main.py`/`anthropic_provider.py`; the kept `ANTHROPIC_API_KEY` env fallback (design decision #11) means the pre-change Claude-only flow keeps working with zero other file changes. |
| 6 — DeepSeek/Groq registry stubs | `node --import tsx --test tests/test_provider_factory.py tests/test_review_endpoint.py` (worker) → **10 pass / 0 fail** (both files); full worker suite **24/24 pass**. | `TestClient` POST `/internal/review` with `provider_name: "deepseek"`/`"groq"` and a fake key → `501 not_implemented`, key never in `detail`; no real DeepSeek/Groq network call anywhere in the test run (no such keys exist in this environment). | Delete `unimplemented_provider.py`; revert the `select_llm_provider` deepseek/groq/unknown branches and `main.py`'s `ProviderNotImplementedError` except-clause — claude-only behavior fully restored. |
| 7 — Angular `admin/` feature | `pnpm --dir apps/web test` → **40 pass / 0 fail** (includes the 21 new `admin-providers-view.test.mjs` tests + 1 extended smoke test); `ng build` typechecks cleanly. | Real `ng serve --proxy-config proxy.conf.json --port 4300 --host 127.0.0.1` against the real running admin API — `GET /admin/llm-providers` → `200` (SPA route renders); proxied `GET /api/v1/admin/llm-providers` with the real secret header → the real masked provider list; manual `curl` end to end (401/403/200 list/201 create/200 activate) confirmed a raw key never appears in any admin response. | Revert `apps/web/src/app/admin/` + the `app.routes.ts` route entry + `apps/web/tests/admin-providers-view.test.mjs` + `apps/web/tests/smoke.test.mjs`'s admin assertions. |
| 8 — Provider provenance on `review_run` + results view | `pnpm --dir apps/api test && pnpm --dir apps/web test` → apps/api **64 pass / 13 skip / 0 fail** (Docker down) / **77 pass / 0 fail** (Docker up, live); apps/web **40 pass / 0 fail**. | `DATABASE_URL=postgres://pg1:pg1@localhost:5433/pg1 node --import tsx --test tests/live-review-integration.test.mjs` — real end-to-end run: a genuinely-completed run's `GET` response names `llm_provider_name: "claude"`/`llm_model_id: "claude-sonnet-4-20250514"`; directly nulling that same run's columns via raw SQL and re-fetching returns a graceful `200`/`null`/`null`, never an error. Also manually confirmed live: a failed run (fake-key rejected by the real Anthropic API) correctly carries `null`/`null` provenance (only populated on completion). | Drop `0003`'s columns via its `-- DOWN`; revert `review-repository.mjs`/`review-orchestrator.mjs`/`live-review-pipeline.mjs`/`api-contract.mjs`/`thesis-api-client.ts`/`results-view.ts`/`results-page.ts`'s provenance additions; delete `review-run-provenance-migration.test.mjs`. |
| 9 — Manual end-to-end verification | N/A per `tasks.md` — not part of `pnpm test`. | Real stack (API + real worker + real `ng serve`, live Postgres) exercised manually: all 4 admin routes, zero-active-provider real `failed`, a full activated-fake-key run genuinely rejected by the real Anthropic API end to end, the Angular admin page served/proxied for real, and a genuine local port-conflict quirk (`localhost:8000` resolving to an unrelated PHP server on `[::1]:8000`) discovered, root-caused, and worked around. The final real-key Claude call remains for the user, exactly as scoped. | N/A — verification + docs only; the runbook section addition is the only artifact, and it is purely additive documentation. |

## Deviations From Design

- None in the schema itself — `llm_provider_config`'s column list matches `design.md`'s Interfaces/Contracts line exactly (`id, provider_name, model_id, encrypted_api_key, api_key_last_four, is_active, metadata, created_at, updated_at`), and the partial unique index matches design decision #3's literal SQL shape (`CREATE UNIQUE INDEX ... ON llm_provider_config (is_active) WHERE is_active`).
- One necessary, flagged update to a pre-existing test's assertion value (12 → 13 expected tables) — see "A necessary, deliberate change to a pre-existing test" above. This is a direct, intended consequence of combining Work Unit 1 (auto-discovery) and Work Unit 2 (new migration) in the same PR, not a design deviation.
- `infra/docker-compose.yml`'s committed port mapping (`5432:5432`) is unchanged from `design.md`'s documented `DATABASE_URL` contract — the temporary `5433` remap was live-verification-only and fully reverted, same as every prior PR in `mvp-vertical-slice`.
- **PR B, flagged (not silent):** `design.md` decision #5 literally says "validated fail-fast at first cipher use." The orchestrator's instructions for this pass asked for fail-fast validation "at API startup." These are not identical, and hooking the check into the shared `bootstrapApi()`/`main()` process bootstrap would have broken the pre-existing, protected `smoke.test.mjs` (which boots the app with no `LLM_PROVIDER_ENCRYPTION_KEY` set and is explicitly out of this pass's scope). Resolution: implemented `design.md`'s literal decision (fail-fast at first cipher use) PLUS an eager fail-fast check at `provider-config-repository.mjs`'s construction time, which fires on the admin subsystem's first request of any kind (not just first write) via `admin-contract.mjs`'s lazy singleton — closer to "startup" for the admin feature specifically, without touching the shared, protected bootstrap. See the full writeup under "Work Unit 3 — A deliberate scope note" above.
- No other deviations. The `llm_provider_config` schema, admin route shapes, masked-response contract, shared-secret gate, and one-active-provider invariant all match `design.md`'s Interfaces/Contracts and `specs/llm-provider-admin/spec.md`'s scenarios exactly.
- **PR C:** none. The zero-active-provider → `review_run.status: "failed"` failure path reuses the pre-existing orchestrator catch block exactly as `design.md`'s Contracts section specifies ("matches the existing failure pattern in review-orchestrator.mjs catch block") — no new error-handling code was written for it. The `ANTHROPIC_API_KEY` env fallback was kept exactly as design decision #11 specifies (arg-then-env precedence, never removed). DeepSeek/Groq fail at `.generate()` time only, never at admin create/activate time, matching design decision #10 literally.
- **PR D, flagged (not silent) — Work Unit 8's provenance columns are a confirmed-not-silent inferred addition.** `design.md`'s File Changes table and Interfaces/Contracts section are genuinely silent on `review_run` provenance columns (re-read verbatim at the start of this pass to confirm literally — see Work Unit 8's write-up above). `specs/llm-provider-admin/spec.md`'s "Backoffice Provider Visibility & Run Provenance" requirement, however, explicitly mandates it: "No existing `review_run`/`finding`/`audit_event` field carries this today; a new field for provider provenance MUST be added and populated." `tasks.md`'s own Work Unit 8 (written before this apply pass) already resolved the exact shape: `0003_review_run_provider_provenance.sql` adding `llm_provider_name`/`llm_model_id` directly to `review_run` — this pass implements that literally, with the column-choice rationale (denormalized snapshot columns vs. a join table or FK) documented explicitly in Work Unit 8 above, not assumed silently.
- **PR D:** otherwise none. `AdminApiClient` as a new sibling client (design decision #8, not extending `ThesisApiClient`) and the in-memory-signal session-scoped secret (design decision #9, not localStorage/build-time) both match `design.md`'s literal decisions. `results-view.ts`'s `ResultsViewModel` gained a field (`providerLabel`) rather than a new `kind` — a minimal, additive extension of the existing discriminated union, not a restructuring.

## Issues Found

- None new from PR A. The local Homebrew-Postgres-on-5432 port conflict is a pre-existing, already-documented environment quirk (see the local port-conflict note above and the archived `mvp-vertical-slice/apply-progress.md`), not a defect introduced by this pass.
- **PR B:** one real bug was caught and fixed during Work Unit 4's GREEN phase — `withRepository()`'s first draft called `getRepository()` (which can throw `EncryptionKeyError` via eager repository construction) outside its own `try` block, so a misconfigured-key request crashed with an uncaught exception instead of returning a handled `5xx` response. Caught by the dedicated RED test for that exact scenario before it could reach a real request; fixed by moving the call inside `try`. No other issues found.
- **PR C:** one test-authoring bug (not a production bug) was caught and fixed during Work Unit 5's RED phase — `review-orchestrator.test.mjs`'s new forwarding test originally imported `defaultRunCagReview` before setting `WORKER_BASE_URL`, so the test hit a nonexistent local server instead of its own fake worker (`DEFAULT_WORKER_BASE_URL` is a top-level `const` evaluated once at import time). Fixed by reordering the import after the env var is set, plus a cache-busting import specifier for safety against the same module being imported earlier in the same file/process. See "A subtle test-authoring bug caught during RED" under Work Unit 5 above. One genuine, predicted production-adjacent issue: `live-review-integration.test.mjs`'s 3 pre-existing scenarios legitimately started failing once active-provider resolution was wired in (correct new behavior, not a bug) — resolved by seeding an active provider in that test's setup, flagged above.
- **PR D:** two things caught and fixed, neither a production bug: (1) `apps/web/tests/smoke.test.mjs`'s first-draft admin assertion over-matched the word "localStorage" inside a doc comment that legitimately explains why it's NOT used — narrowed the regex to actual usage syntax rather than weakening the security-relevant comment (see "A necessary correction caught by the harness itself" under Work Unit 7 above). (2) an ad-hoc multi-file `node --test` invocation without the project's own `--test-concurrency=1` flag produced a spurious concurrent-migration-race failure — root-caused as a test-harness-invocation mistake on this pass's part (not a real bug), re-verified clean with the correct flag (see the flagged note under Work Unit 8 above). One genuine local-machine discovery (not a code bug, but worth recording precisely because it could otherwise be mistaken for one): `localhost:8000` resolving to an unrelated local PHP dev server on `[::1]:8000` instead of the real worker on `127.0.0.1:8000` during manual verification — root-caused, worked around, and documented in the runbook (see Work Unit 9 above and the new "local port-conflict note" under Test Commands Run).

## Remaining Tasks

None. Work Units 1-9 are all complete — see "Change Status" below. `llm-provider-admin` is ready for `sdd-verify`; the only step that could not be executed inside this session is the final real-`ANTHROPIC_API_KEY` manual acceptance step in `docs/mvp-vertical-slice-runbook.md`'s new "LLM Provider Admin" section, which requires the user's own real key entered through the UI (never `.env`), exactly as scoped by design and by every prior PR in this change.

## Verification Evidence

- Safety-net verification command: `pnpm test` (root, before any change) → `apps/api` 45 pass / 7 skip / 0 fail, `apps/web` 15 pass / 0 fail, `services/worker` 13 tests OK — confirms this pass starts from `mvp-vertical-slice`'s exact archived final state.
- Work Unit 1 RED verification: `node --import tsx --test tests/migrate-runner.test.mjs` (Docker down) → `TypeError: listMigrationFiles is not a function` (test 3 failed as expected; tests 1–2 pre-existing pass; tests 4–5 live-pg skip with explicit reason).
- Work Unit 1 GREEN verification (offline): same command → 3 pass / 0 fail / 2 skip.
- Work Unit 2 RED verification (live pg, port 5433 workaround): `DATABASE_URL=postgres://pg1:pg1@localhost:5433/pg1 node --import tsx --test tests/llm-provider-config-migration.test.mjs` → 1 fail, `0 !== 1` (table did not exist).
- Work Unit 2 GREEN verification (live pg): same command, after creating `0002_llm_provider_config.sql` → 1 pass / 0 fail.
- Combined TRIANGULATE verification (live pg): `DATABASE_URL=postgres://pg1:pg1@localhost:5433/pg1 node --import tsx --test tests/migrate-runner.test.mjs` → **6 pass / 0 fail**, zero skips (every live-pg test genuinely ran).
- Full `apps/api` suite verification (live pg): `DATABASE_URL=postgres://pg1:pg1@localhost:5433/pg1 pnpm --dir apps/api test` → **56 pass / 0 fail**.
- Manual CLI up/down verification: `node apps/api/src/db/migrate.mjs down` → `... up` → `docker exec pg1-db psql -U pg1 -d pg1 -c "\dt"` → 13 rows (12 baseline + `llm_provider_config`) → `... down` → `\dt` → "Did not find any relations."
- `docker compose -f infra/docker-compose.yml down -v` confirmed to cleanly remove the container, network, and volume (rollback boundary for the live-verification session).
- `infra/docker-compose.yml`'s temporary port remap (`5433:5432` for verification only) confirmed fully reverted via `git diff infra/docker-compose.yml` → empty diff.
- Final `apps/api`-alone verification (Docker down, canonical port): `pnpm --dir apps/api test` → **46 pass / 10 skip / 0 fail**.
- Final root verification command (Docker down, canonical port, PR A state): `pnpm test` (root) → **exit code 0**; `apps/api` 46 pass / 10 skip / 0 fail, `apps/web` 15 pass / 0 fail, `services/worker` 13 tests OK.
- **PR B** cipher RED verification: `node --import tsx --test tests/provider-key-cipher.test.mjs` (pre-GREEN) → `ERR_MODULE_NOT_FOUND`, **12/12 fail**.
- PR B cipher GREEN verification: same command, after creating `provider-key-cipher.mjs` → **12 pass / 0 fail** (Docker down; identical result with Docker up).
- PR B admin-contract RED verification: `node --import tsx --test tests/admin-contract.test.mjs` (pre-GREEN) → `ERR_MODULE_NOT_FOUND` on 5/6 tests; the 6th (live-DB) correctly `t.skip`s (Docker not up).
- PR B admin-contract GREEN verification (offline): same command, after creating `admin-contract.mjs`/`provider-config-repository.mjs` → 5 pass / 0 fail / 1 correctly-skip.
- PR B admin-contract GREEN verification (live pg, port 5433 workaround): `DATABASE_URL=postgres://pg1:pg1@localhost:5433/pg1 node --import tsx --test tests/admin-contract.test.mjs` → **6 pass / 0 fail / 0 skip**.
- PR B full `apps/api` suite verification (live pg): `DATABASE_URL=postgres://pg1:pg1@localhost:5433/pg1 pnpm --dir apps/api test` → **74 pass / 0 fail**.
- PR B `contract.test.mjs`/`smoke.test.mjs` protection re-verification (live pg, real `Pg1ApiModule` boot including `AdminController`): `node --import tsx --test tests/smoke.test.mjs tests/contract.test.mjs` → **14 pass / 0 fail**; `smoke.test.mjs` boots successfully with no `LLM_PROVIDER_ENCRYPTION_KEY` set, confirming the design-deviation resolution (fail-fast at first cipher use / admin-subsystem's first request, not at the shared process bootstrap) does not regress unrelated tests.
- PR B manual runtime-harness verification: real server (`node --import tsx src/main.ts`, port 3099, live pg) + `curl` against all 4 admin routes — 401/403/200/422/201/200/200 all confirmed exactly as expected, with manual inspection confirming no raw or encrypted key ever appeared in any response body.
- PR B post-refactor re-verification (offline): `node --import tsx --test tests/provider-key-cipher.test.mjs tests/admin-contract.test.mjs tests/contract.test.mjs` → **29 pass / 1 skip (expected) / 0 fail** — confirms the validation-helper extraction changed no behavior.
- `docker compose -f infra/docker-compose.yml down -v` (PR B) confirmed to cleanly remove the container, network, and volume.
- `infra/docker-compose.yml`'s temporary port remap (PR B, `5433:5432` for verification only) confirmed fully reverted via `git diff infra/docker-compose.yml` → empty diff.
- Final full root verification command (Docker down, canonical port, PR B state): `pnpm test` (root) → **exit code 0**; `apps/api` **63 pass / 11 skip / 0 fail** (74 total), `apps/web` 15 pass / 0 fail, `services/worker` 13 tests OK.
- **PR C** worker RED verification: `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. python3 -m unittest discover -s tests -p 'test_*.py'` (pre-GREEN) → **24 tests run, 9 failures/errors** (`failures=2, errors=7`): 2 precedence-test failures in `test_cag_review.py`, 6 `ImportError`s in `test_provider_factory.py`, 2 status-code mismatches (`500 != 501`) in `test_review_endpoint.py` (its other 2 tests already passed, since they cover pre-existing/unrelated behavior).
- PR C worker GREEN verification: same command, after all worker-side changes → **24 pass / 0 fail**.
- PR C `apps/api` (no-DB) RED/GREEN verification: `node --import tsx --test tests/review-orchestrator.test.mjs` (Docker down) → RED: `SyntaxError` from an import-ordering test-authoring bug, fixed, re-confirmed as a genuine `deepEqual` mismatch; GREEN (after implementation): **4 pass / 0 fail** (3 correctly-skip live-pg tests count as skip, not fail, in the file's overall total — this file's 4th test is the new one, pass).
- PR C `active-provider-resolution.test.mjs` RED verification (live pg, port 5433 workaround): `DATABASE_URL=postgres://pg1:pg1@localhost:5433/pg1 node --import tsx --test tests/active-provider-resolution.test.mjs` → 1 fail, `AssertionError: expected 'failed', actual: 'completed'`.
- PR C `active-provider-resolution.test.mjs` GREEN verification (live pg): same command, after `live-review-pipeline.mjs`'s GREEN → **1 pass / 0 fail** (all 4 sequential scenarios).
- PR C `live-review-integration.test.mjs` predicted-regression verification (live pg): confirmed a genuine `AssertionError: expected 'completed', actual: 'failed'` on the unmodified test against the post-GREEN tree, then fixed by seeding an active provider — re-ran, **1 pass / 0 fail**.
- PR C full `apps/api` suite verification (live pg): `DATABASE_URL=postgres://pg1:pg1@localhost:5433/pg1 pnpm --dir apps/api test` → **76 pass / 0 fail / 0 skip**.
- PR C `contract.test.mjs`/`smoke.test.mjs` protection re-verification (live pg): `node --import tsx --test tests/smoke.test.mjs tests/contract.test.mjs` → **14 pass / 0 fail**; `git diff` confirms both files byte-untouched.
- `docker compose -f infra/docker-compose.yml down -v` (PR C) confirmed to cleanly remove the container, network, and volume.
- `infra/docker-compose.yml`'s temporary port remap (PR C, `5433:5432` for verification only) confirmed fully reverted via `git diff infra/docker-compose.yml` → empty diff.
- Final full root verification command (Docker down, canonical port, PR C state): `pnpm test` (root) → **exit code 0**; `apps/api` **64 pass / 12 skip / 0 fail** (76 total), `apps/web` 15 pass / 0 fail, `services/worker` **24 tests OK**.
- **PR D** Work Unit 7 RED verification: `node --import tsx --test tests/admin-providers-view.test.mjs` (pre-GREEN) → `ERR_MODULE_NOT_FOUND`.
- PR D Work Unit 7 GREEN verification: same command, after creating the `admin/` module set → **21 pass / 0 fail**; full `apps/web` suite → **37 pass / 0 fail**; `ng build --configuration development` → bundle built successfully.
- PR D Work Unit 8 RED verification (live pg, port 5433 workaround), all four fronts: `review-run-provenance-migration.test.mjs` → `0 !== 2`; `review-repository.test.mjs` → `null !== 'claude'`; `review-orchestrator.test.mjs` → `llmProviderName: null`; `live-review-integration.test.mjs` → `undefined !== 'claude'` — all genuine, pre-GREEN failures.
- PR D Work Unit 8 GREEN verification (live pg), all four fronts: **1/1**, **2/2**, **4/4**, **1/1** pass respectively; full `apps/api` suite (live) → **77 pass / 0 fail / 0 skip**.
- PR D Work Unit 8 web-side RED/GREEN verification: `results-view.test.mjs` → RED `SyntaxError` (missing export), GREEN **11 pass / 0 fail**; full `apps/web` suite → **40 pass / 0 fail**.
- PR D `contract.test.mjs`/`smoke.test.mjs` protection re-verification (live pg): `node --import tsx --test tests/smoke.test.mjs tests/contract.test.mjs` → **14 pass / 0 fail**; both byte-untouched.
- PR D migration-concurrency false-alarm re-check (live pg, correct `--test-concurrency=1` flag): `migrate-runner.test.mjs` + `llm-provider-config-migration.test.mjs` + `review-run-provenance-migration.test.mjs` together → **8 pass / 0 fail** — confirms the earlier ad-hoc-invocation failure was a harness mistake, not a real bug.
- PR D manual runtime-harness verification (live pg + real worker + real `ng serve`): all 4 admin routes behave exactly as automated tests expect; zero-active-provider run reaches a real `failed`; a full activated-but-fake-key run is genuinely rejected end to end by the real Anthropic API (`401 invalid x-api-key`, captured in the worker's own log), surfacing as `failed` with graceful `null`/`null` provenance; `/admin/llm-providers` renders via the real dev server and its proxied admin API call returns the real masked list; the `localhost:8000`-resolves-to-an-unrelated-PHP-server quirk was found, root-caused, and worked around (see the new local port-conflict note under Test Commands Run and Work Unit 9 above).
- `node .../migrate.mjs down` (PR D) confirmed clean schema teardown before Docker teardown.
- `docker compose -f infra/docker-compose.yml down -v` (PR D) confirmed to cleanly remove the container, network, and volume.
- `infra/docker-compose.yml`'s temporary port remap (PR D, `5433:5432` for verification only) confirmed fully reverted via `git diff infra/docker-compose.yml` → empty diff.
- Final `apps/api`-alone verification (Docker down, canonical port, PR D state): `pnpm --dir apps/api test` → **64 pass / 13 skip / 0 fail** (77 total).
- Final full root verification command (Docker down, canonical port, PR D / final state): `pnpm test` (root) → **exit code 0**; `apps/api` **64 pass / 13 skip / 0 fail**, `apps/web` **40 pass / 0 fail**, `services/worker` **24 tests OK**.

## Change Status

**PR A (Work Units 1–2), PR B (Work Units 3–4), PR C (Work Units 5–6), and PR D (Work Units 7–9) of `llm-provider-admin` are ALL complete. This was the final PR of the 4-PR chain.**

PR A: Migration runner auto-discovery is implemented, tested (unit + live-Postgres integration, including a genuine FK-ordering proof for both UP and DOWN directions), and verified against real dockerized Postgres via both the automated suite and a manual CLI cycle cross-checked with `psql \dt`. The `llm_provider_config` table, its CHECK constraints, and its exactly-one-active partial unique index are all live-verified.

PR B: AES-256-GCM at-rest encryption (Node built-in `node:crypto`, no new dependency) is implemented with fail-fast key validation, tamper detection (GCM auth tag), and 12 passing unit tests. Admin CRUD + activate endpoints are implemented behind a documented-temporary `x-admin-secret` shared-secret gate (constant-time compare, both in the isolated pure `admin-contract.mjs` and in a real NestJS `AdminSecretGuard`), with masked-key-only responses (`api_key_last_four`, never raw or encrypted key material), atomic single-active-provider enforcement, and unsupported-provider-name rejection — all live-verified against real dockerized Postgres via both the automated suite (6/6 admin-contract tests, 74/74 full `apps/api` suite) and manual `curl` against all 4 routes through the real running server. `contract.test.mjs` remains byte-untouched and green throughout.

PR C: Every review-run trigger now resolves+decrypts the DB-active LLM provider fresh (never cached across triggers) and forwards `provider_name`/`api_key`/`model_id` into the worker's `/internal/review` request body; zero active providers reaches a real `review_run.status: "failed"` with an explicit `error_summary` via the pre-existing orchestrator catch block (no new error-handling code needed, matching `design.md`'s Contracts section literally). The worker's `AnthropicProvider` now accepts an explicit `api_key`/`model`, preferring it over `ANTHROPIC_API_KEY` (arg-then-env precedence, design decision #11 — the env fallback is fully preserved for rollback/local dev, never removed). DeepSeek and Groq are registered, selectable providers whose `.generate()` call raises `ProviderNotImplementedError`, surfaced at the real HTTP boundary as `501 not_implemented` — proven at both the pure-factory level and the full FastAPI `TestClient` level, with an explicit no-key-leak assertion in every failure-path test. Live-verified end to end against real dockerized Postgres: zero-active-provider failure, per-run re-resolution across a provider switch (no restart), and a downstream worker failure that never leaks the active provider's raw key into `error_summary`. A necessary, flagged update seeded an active provider into the pre-existing `live-review-integration.test.mjs` (its scenarios test worker-response handling, not provider resolution, and would otherwise now fail for the wrong reason). `contract.test.mjs`/`smoke.test.mjs` remain byte-untouched and green. Full suite: `apps/api` 76/0/0 live, 64/12/0 offline; `apps/web` 15/0/0 (unchanged); `services/worker` 24/0/0 (up from 13).

PR D: An Angular `admin/` feature (`apps/web/src/app/admin/`) provides a full backoffice for `llm_provider_config` — a masked provider list (last-four key, active/inactive badge, provider name, model id), a single add/edit form whose raw API key field is write-only (cleared after every successful save, never pre-filled on edit), and a per-row activate action — all standalone/OnPush/`inject()`/native-control-flow, exactly matching `upload-page.ts`/`results-page.ts`'s established conventions. A new sibling `AdminApiClient` (design decision #8) sends the required `x-admin-secret` header on every admin request, resolved via a new `AdminSecretStore` holding the secret in a session-scoped, in-memory-only Angular signal (design decision #9 — never `localStorage`, never build-time baked; a `403` clears it so the next action re-prompts). Provider provenance is now recorded and surfaced end to end: a new additive `0003_review_run_provider_provenance.sql` migration adds nullable `llm_provider_name`/`llm_model_id` columns to `review_run` (the location/shape decision — resolving `design.md`'s genuine silence on this, per `specs/llm-provider-admin/spec.md`'s explicit "Backoffice Provider Visibility & Run Provenance" requirement — is documented explicitly in Work Unit 8 above, not silently assumed); populated by the orchestrator on completion from the resolved active provider (the worker's own response never echoes this back); surfaced on every `GET /review-runs/{id}` response and rendered on the results page as "Reviewed by: `<provider> (<model id>)`", with a graceful "Unknown provider" fallback for runs with no recorded provenance — proven at the pure-unit, repository, and full-live-HTTP layers, including a real genuinely-completed run whose columns were directly nulled and re-fetched. Work Unit 9's manual end-to-end verification ran everything short of the actual real-key Claude call for real this pass: server boot with the new admin env vars, all 4 admin endpoints via real `curl`, the Angular admin page served and proxied by a real `ng serve`, a zero-active-provider run reaching a real `failed` status, and a full activated-(fake-key) provider run genuinely rejected end to end by the real Anthropic API (`401 invalid x-api-key`) — proving the entire admin→pipeline→worker→Claude wiring is real. A genuine local-machine port-conflict quirk (`localhost:8000` resolving to an unrelated PHP dev server on `[::1]:8000` instead of the real worker on `127.0.0.1:8000`) was discovered, root-caused, worked around, and documented in the runbook, alongside a new "LLM Provider Admin — Manual End-to-End Verification" runbook section for the user's final real-key acceptance step. `contract.test.mjs`/`smoke.test.mjs` remain byte-untouched and green. Full suite: `apps/api` 77/0/0 live, 64/13/0 offline (up from PR C's 76/0/0 and 64/12/0 by the 1 new live-pg-gated migration test); `apps/web` 40/0/0 (up from 15/0/0 by the 21 new admin-view tests + 1 extended smoke test + 3 new provenance tests + 1 modified-in-place set); `services/worker` 24/0/0 (unchanged — no worker changes in this PR).

**`llm-provider-admin` is fully implemented, tested, and live-verified. The only remaining action is the user's own manual, real-`ANTHROPIC_API_KEY` acceptance step documented in `docs/mvp-vertical-slice-runbook.md`'s new "LLM Provider Admin" section — everything up to and including that step's plumbing has been proven real, not assumed, in this session.**

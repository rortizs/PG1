/**
 * NestJS-compatible bootstrap placeholder for the PG1 API.
 *
 * This slice intentionally avoids product endpoints. Real NestJS modules are
 * introduced in the API contract work unit after the scaffold is verified.
 */
export function bootstrapApi(): string {
  return 'pg1-api-scaffold';
}
